# v0.4.0 등산 카테고리 + 전면 업그레이드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 등산(hike) 카테고리를 산 검색·등산 점수·하산/일출/낙뢰/돌풍/조망/준비물 안전 카드·새벽 시간대와 함께 추가하고, 날씨 데이터를 확장하고, 카테고리 순서/문구/이모지/설명을 정비하고, 전 기기 반응형을 완성한다.

**Architecture:** 기존 활동 프로필 시스템에 `hike`를 추가한다. 날씨 fetch에 등산 안전용 변수(낙뢰·돌풍·조망·눈)를 optional 필드로 추가하되 기존 점수 계산은 이를 읽지 않아 러닝 골든마스터가 불변이다. 등산 전용 안전 카드는 `getHikePlan`이 계산하고, 시간대(새벽 포함)·검색(산 우선)은 활동별로 분기한다.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest, Open-Meteo(weather+air), Kakao Local API.

## Global Constraints

- **러닝 불변**: run 점수·톤·멘트 완전 불변. 새 날씨 필드는 slot 점수에 미사용. `pnpm test` 골든마스터 통과 못 하면 배포 금지.
- **카피 격리**: 활동 화면에 다른 활동 표현 안 샘. 완료 후 grep 검증.
- **데이터 보존**: localStorage 키 유지. 마지막 선택 활동 기억.
- 활동 순서: `["walk", "hike", "dog", "run", "bike"]`. 신규 기본 탭 = walk.
- 새 파일 첫 줄 한글 주석. 한글 문장 마침표 종결.
- 커밋 트레일러: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- PATH: `export PATH="/opt/homebrew/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"` + `cd "/Users/runner706/Developer/러닝콜"`.

---

## File Structure

- **Modify** `lib/scoring.ts` — HourlyInput에 optional 날씨 필드, COMMENTS.hike.
- **Modify** `lib/weather.ts` — Open-Meteo 변수 추가, HourlyInput 새 필드 파싱.
- **Modify** `lib/activity.ts` — hike 프로필, ACTIVITY_ORDER, terms, `getHikePlan`/`HikePlan`.
- **Modify** `lib/insights.ts` — hike 콘텐츠, 활동별 dayparts(hike 4구간)·ranked window 새벽.
- **Modify** `lib/outfit.ts` — 등산 준비물.
- **Modify** `app/api/search-location/route.ts` — kind/categoryName, 산 우선 정렬.
- **Modify** `app/page.tsx` — hike 안전 카드, tagline 노출, 기본탭 walk, 검색 활동화, daypart 개수.
- **Modify** `app/globals.css` — hike 카드·4시간대·tagline·반응형.
- **Modify** `lib/__tests__/scoring.test.ts` — hike 테스트.

---

## Task 1: weather.ts — 등산 안전용 날씨 데이터 확장

**Files:**
- Modify: `lib/scoring.ts` (HourlyInput 타입)
- Modify: `lib/weather.ts`

**Interfaces:**
- Produces: `HourlyInput`에 optional `weatherCode?, windGust?, visibility?, cloudCover?, snowfall?: number`. fetch가 이들을 채움.

- [ ] **Step 1: HourlyInput에 optional 필드 추가 (scoring.ts)**

`lib/scoring.ts`의 `HourlyInput` 타입에 추가:
```ts
export type HourlyInput = {
  time: string;
  temperature: number;
  apparentTemperature: number;
  humidity: number;
  pm10: number;
  pm25: number;
  uvIndex: number;
  precipitation: number;
  precipitationProbability: number;
  windSpeed: number;
  // 등산 안전용 (optional, slot 점수엔 미사용 — 러닝 불변 보장)
  weatherCode?: number;
  windGust?: number;
  visibility?: number;
  cloudCover?: number;
  snowfall?: number;
};
```

- [ ] **Step 2: weather.ts fetch 변수 추가**

`fetchRawForecast`의 `hourly` 파라미터에 변수 추가:
```ts
    hourly:
      "temperature_2m,apparent_temperature,relative_humidity_2m,uv_index,precipitation,precipitation_probability,wind_speed_10m,wind_gusts_10m,weather_code,visibility,cloud_cover,snowfall",
```
그리고 `WeatherResponse["hourly"]` 타입에 필드 추가:
```ts
    wind_gusts_10m?: Array<number | null>;
    weather_code?: Array<number | null>;
    visibility?: Array<number | null>;
    cloud_cover?: Array<number | null>;
    snowfall?: Array<number | null>;
```
`allInputs` 매핑의 input 객체에 추가:
```ts
      windGust: ensureNumber(weather.hourly?.wind_gusts_10m?.[index]),
      weatherCode: ensureNumber(weather.hourly?.weather_code?.[index]),
      visibility: ensureNumber(weather.hourly?.visibility?.[index], 20000),
      cloudCover: ensureNumber(weather.hourly?.cloud_cover?.[index]),
      snowfall: ensureNumber(weather.hourly?.snowfall?.[index])
```

- [ ] **Step 3: 검증**

Run: `pnpm test && pnpm typecheck`
Expected: 골든마스터 통과(새 필드는 점수 계산에 안 쓰이므로 run 불변), 타입 OK.

- [ ] **Step 4: Commit**

```bash
git add lib/scoring.ts lib/weather.ts
git commit -m "feat: 등산 안전용 날씨 데이터 확장 (낙뢰·돌풍·조망·눈)

HourlyInput optional 필드 + Open-Meteo 변수. 점수 계산 불변.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: activity.ts — hike 프로필 + 순서 + getHikePlan

**Files:**
- Modify: `lib/activity.ts`
- Modify: `lib/scoring.ts` (COMMENTS.hike)
- Modify: `lib/__tests__/scoring.test.ts`

**Interfaces:**
- Produces:
  - `ActivityKey = "walk" | "hike" | "dog" | "run" | "bike"`
  - `ACTIVITY_ORDER = ["walk","hike","dog","run","bike"]`
  - `ACTIVITIES.hike`
  - `getHikePlan(input): HikePlan`, `type HikePlan`

- [ ] **Step 1: ActivityKey + hike 프로필 + 순서**

`lib/activity.ts`: `ActivityKey`에 `"hike"` 추가. `ACTIVITIES`에 hike 추가:
```ts
  hike: {
    key: "hike", label: "등산", emoji: "⛰️", tagline: "산 오르기 좋은 시간과 하산 여유까지",
    ready: true, resultMode: "score",
    weights: { dust: 0.12, temperature: 0.18, precipitation: 0.28, uv: 0.1, humidity: 0.1, wind: 0.22 },
    temp: { optimalLo: 6, optimalHi: 18, coldSlope: 4.5, hotSlope: 5 },
    heat: { hot1: 31, hot1Cap: 55, hot2: 34, hot2Cap: 38 },
    windCap: { speed: 12, cap: 38 },
    terms: { verbDo: "등산하", outfitTitle: "등산 준비물", alarmTitle: "산행 알림", rainyTag: "우중 산행", blockedTag: "산행 위험", nowTag: "지금 산행 딱!", notifReady: "산 오르기 좋은 시간이에요" }
  }
```
`ACTIVITY_ORDER = ["walk", "hike", "dog", "run", "bike"];`
다른 활동 tagline도 갱신:
- walk: "가볍게 걷기 좋은 시간을 알려드려요"
- dog: "강아지 발바닥까지 생각한 산책 타이밍"
- run: "달리기 좋은 순간을 짚어드려요"
- bike: "바람까지 계산한 라이딩 타이밍"

- [ ] **Step 2: getHikePlan + HikePlan (activity.ts 끝에)**

```ts
export type HikeSignal = { level: "danger" | "caution" | "info"; emoji: string; text: string };
export type HikePlan = {
  descentDeadline: string | null; // "늦어도 오후 5시 30분"
  sunsetText: string | null;      // 일몰 표시
  sunrisePlan: string | null;     // 새벽 산행 안내
  signals: HikeSignal[];
  view: string;                   // 조망
  summitNote: string;             // 정상 체감
  checklist: string[];
};

function fmtAmPmKo(hour: number, minute: number) {
  const ap = hour < 12 ? "오전" : "오후";
  const h = hour % 12 === 0 ? 12 : hour % 12;
  const m = minute > 0 ? ` ${minute}분` : "";
  return `${ap} ${h}시${m}`;
}

function parseClock(iso: string | null): { h: number; m: number } | null {
  if (!iso) return null;
  const h = Number(iso.slice(11, 13));
  const m = Number(iso.slice(14, 16));
  if (!Number.isFinite(h)) return null;
  return { h, m: Number.isFinite(m) ? m : 0 };
}

export function getHikePlan(input: {
  hour: number;
  temperature: number; apparentTemperature: number;
  windSpeed: number; windGust?: number; precipitation: number; precipitationProbability: number;
  weatherCode?: number; visibility?: number; cloudCover?: number; snowfall?: number; pm25: number;
  sunrise: string | null; sunset: string | null;
}): HikePlan {
  // 하산 마감 = 일몰 2.5시간 전
  const set = parseClock(input.sunset);
  let descentDeadline: string | null = null;
  let sunsetText: string | null = null;
  if (set) {
    sunsetText = `일몰 ${fmtAmPmKo(set.h, set.m)}`;
    let dh = set.h; let dm = set.m - 30;
    dh -= 2; if (dm < 0) { dm += 60; dh -= 1; }
    descentDeadline = `${fmtAmPmKo((dh + 24) % 24, dm)}엔 하산 시작`;
  }
  // 일출 산행 (이른 시간)
  const rise = parseClock(input.sunrise);
  let sunrisePlan: string | null = null;
  if (rise && input.hour <= 8) {
    sunrisePlan = `일출 ${fmtAmPmKo(rise.h, rise.m)} · 정상 일출 보려면 여유 있게 출발하세요`;
  }

  const signals: HikeSignal[] = [];
  if (input.weatherCode !== undefined && input.weatherCode >= 95) {
    signals.push({ level: "danger", emoji: "⛈️", text: "낙뢰 위험. 능선·정상은 매우 위험하니 산행을 미루세요." });
  }
  const gust = input.windGust ?? input.windSpeed;
  if (gust >= 14) {
    signals.push({ level: "danger", emoji: "💨", text: "돌풍이 강해요. 정상부 저체온·실족 위험이 커요." });
  } else if (input.windSpeed >= 9) {
    signals.push({ level: "caution", emoji: "🌬️", text: "능선 바람이 강해요. 정상부는 체감이 더 낮아요." });
  }
  if (input.precipitation >= 0.5 || input.precipitationProbability >= 60) {
    signals.push({ level: "caution", emoji: "🌧️", text: "비 소식이 있어요. 바위·흙길·계단 미끄럼을 조심하세요." });
  }
  if ((input.snowfall ?? 0) > 0 || (input.temperature <= 0 && input.humidity >= 70)) {
    signals.push({ level: "caution", emoji: "❄️", text: "결빙·눈 가능. 아이젠과 스틱을 챙기세요." });
  }

  // 조망 (visibility m, cloudCover %, 미세먼지)
  const vis = input.visibility ?? 20000;
  const view =
    vis < 5000 || input.pm25 > 55
      ? "오늘은 조망이 흐릴 수 있어요."
      : (input.cloudCover ?? 0) >= 80
      ? "구름이 많아 조망은 아쉬울 수 있어요."
      : vis >= 15000 && input.pm25 <= 35
      ? "조망이 트인 맑은 날이에요."
      : "조망은 무난한 편이에요.";

  const summitNote = "정상부는 기온보다 6~10°C 낮고 바람이 강해요. 겹쳐 입으세요.";

  const checklist: string[] = ["💧 충분한 물", "🧥 바람막이", "🔋 보조배터리"];
  if (input.uvIndex >= 5) checklist.push("🧢 모자·선크림");
  if (input.precipitation >= 0.3 || input.precipitationProbability >= 60) checklist.push("🌂 우비");
  if (input.hour >= 14 || (set && input.hour >= set.h - 3)) checklist.push("🔦 헤드랜턴");
  if ((input.snowfall ?? 0) > 0 || input.temperature <= 2) checklist.push("🧊 아이젠·장갑");

  return { descentDeadline, sunsetText, sunrisePlan, signals: signals.slice(0, 4), view, summitNote, checklist };
}
```
> 주의: `input.uvIndex`가 파라미터에 있어야 checklist에서 참조 가능 — 위 input 타입에 `uvIndex: number; humidity: number;` 추가. (아래 Step에서 반영.)

- [ ] **Step 2b: input 타입 보정**

Step 2의 `getHikePlan` input 타입에 `uvIndex: number;` 와 `humidity: number;` 를 포함시킨다(누락 방지). 최종 input 타입:
```ts
  hour: number; temperature: number; apparentTemperature: number; humidity: number;
  windSpeed: number; windGust?: number; precipitation: number; precipitationProbability: number;
  uvIndex: number; weatherCode?: number; visibility?: number; cloudCover?: number; snowfall?: number; pm25: number;
  sunrise: string | null; sunset: string | null;
```

- [ ] **Step 3: COMMENTS.hike (scoring.ts)**

`COMMENTS`에 hike 추가:
```ts
  hike: {
    excellent: ["산 오르기 최고예요", "완벽한 산행 날씨예요", "조망까지 트인 날이에요", "이런 날 산 안 가면 아까워요"],
    good: ["산행하기 좋아요", "오르기 좋은 날이에요", "기분 좋은 산행이 될 거예요", "부담 없이 다녀오기 좋아요"],
    fair: ["무난한 산행이에요", "가벼운 코스가 어울려요", "낮은 산은 괜찮아요", "컨디션 보며 올라요"],
    caution: ["짧은 코스만 추천해요", "무리한 정상은 피해요", "날씨 변화 주의해요", "가까운 둘레길이 좋아요"],
    bad: ["오늘 산행은 위험해요", "무리할 날은 아니에요", "다음 기회를 노려요", "안전이 우선이에요"]
  }
```

- [ ] **Step 4: hike 테스트 (scoring.test.ts)**

"활동별 점수 차이" describe에 추가:
```ts
  it("강풍·비일 때 등산 점수가 크게 낮다", () => {
    const bad = { time: "2026-04-01T13:00", temperature: 10, apparentTemperature: 9, humidity: 60, pm10: 30, pm25: 15, uvIndex: 3, precipitation: 2, precipitationProbability: 90, windSpeed: 13 };
    const clear = { ...bad, precipitation: 0, precipitationProbability: 0, windSpeed: 2 };
    expect(calculateSlot(clear as any, ACTIVITIES.hike).totalScore - calculateSlot(bad as any, ACTIVITIES.hike).totalScore).toBeGreaterThanOrEqual(30);
  });
```

- [ ] **Step 5: 검증 + Commit**

Run: `pnpm test && pnpm typecheck`
Expected: 골든마스터 + 활동차이 전부 PASS.
```bash
git add lib/activity.ts lib/scoring.ts lib/__tests__/scoring.test.ts
git commit -m "feat: 등산 프로필 + getHikePlan(하산/일출/낙뢰/조망/준비물) + 순서 변경

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: insights.ts — 등산 콘텐츠 + 새벽 시간대

**Files:**
- Modify: `lib/insights.ts`

**Interfaces:**
- Consumes: `ActivityKey`("hike").
- Produces: hike 키 추가된 콘텐츠 테이블. `getDayParts`/`getRankedWindows`가 hike에서 새벽 포함.

- [ ] **Step 1: 시간대 활동별 분기 (dayparts + ranked window)**

`getRankedWindows` 시작 시각을 활동별로: 함수 시그니처에 `activity: ActivityKey = "run"` 추가하고 루프 시작을 `const startH = activity === "hike" ? 5 : 6;` 로. (호출부 page.tsx에서 activity 전달 — Task 6.)

`getDayParts`의 DAY_PARTS를 활동별로. hike는 4구간:
```ts
const HIKE_DAY_PARTS: Array<{ key: DayPartKey; label: string; icon: string; range: [number, number] }> = [
  { key: "dawn", label: "새벽", icon: "sunrise", range: [5, 8] },
  { key: "morning", label: "아침", icon: "sunrise", range: [8, 11] },
  { key: "day", label: "낮", icon: "day", range: [11, 15] },
  { key: "evening", label: "저녁", icon: "sunset", range: [15, 19] }
];
```
`DayPartKey`에 `"dawn"` 추가. `getDayParts`에서 `const defs = activity === "hike" ? HIKE_DAY_PARTS : DAY_PARTS;` 로 선택하고 desc는 `DAY_PART_DESC[activity]`가 4개일 수 있으니 hike desc 배열은 4개로:
```ts
  hike: ["새벽 산행", "아침 산행", "한낮 산행", "저녁 산행"],
```
(다른 활동은 3개 유지. desc 매핑은 index 기반이므로 hike만 4개 index 지원.)

- [ ] **Step 2: HEADLINE_SCORE.hike + SUBLINES.hike + ONE_LINERS.hike + BRIEFING_VERDICT.hike + DAY_PART_DESC.hike**

각 테이블에 hike 키 추가. 예시(HEADLINE_SCORE.hike):
```ts
  hike: [
    [88, ["산 오르기 최고예요", "완벽한 산행 날씨예요", "조망 트인 날이에요"]],
    [80, ["산행하기 좋아요", "오르기 좋은 날이에요", "기분 좋은 날이에요"]],
    [72, ["산행 무난해요", "오를 만해요", "나쁘지 않아요"]],
    [55, ["보통이에요", "가벼운 코스라면", "낮은 산은 괜찮아요"]],
    [38, ["살짝 아쉬워요", "무리는 금물이에요", "둘레길이 나아요"]],
    [0, ["오늘은 위험해요", "실내가 나아요", "산행 어려워요"]]
  ]
```
SUBLINES.hike(rain/rainMaybe/dustBad/dustSoso/hot2/hot1/cold2/cold1/windy/uv/humid/score) — 등산 톤:
```ts
  hike: {
    rain: ["오늘 산행은 미뤄요", "바위·계단 미끄럼 위험", "능선은 특히 조심해요"],
    rainMaybe: ["우비 꼭 챙겨요", "정상 날씨 확인해요", "낮은 코스로 짧게"],
    dustBad: ["조망도 건강도 아쉬워요", "무리한 정상은 피해요", "마스크 챙기면 좋아요"],
    dustSoso: ["조망은 살짝 흐려요", "가벼운 코스로요", "물 자주 마셔요"],
    hot2: ["이른 시간에 올라요", "물 넉넉히 챙겨요", "그늘 코스로요"],
    hot1: ["수분 자주 챙겨요", "천천히 페이스로", "정상은 시원해요"],
    cold2: ["정상부 저체온 주의", "방한 단단히 해요", "짧게 다녀와요"],
    cold1: ["겹쳐 입고 올라요", "장갑 챙겨요", "능선 바람 대비해요"],
    windy: ["능선·정상 강풍 주의", "돌풍 실족 조심해요", "무리한 능선 피해요"],
    uv: ["모자·선크림 챙겨요", "능선은 그늘 없어요", "선글라스면 편해요"],
    humid: ["땀 많이 나요", "여벌 옷 챙겨요", "천천히 올라요"],
    score: [
      [88, ["일출·조망 노려봐요", "정상까지 여유 있어요", "하산 시간만 챙기면 완벽"]],
      [80, ["편한 코스로 즐겨요", "중간 봉우리도 좋아요", "물·간식 챙겨 가요"]],
      [72, ["부담 없이 다녀와요", "낮은 산도 좋아요", "하산 시간 확인해요"]],
      [55, ["둘레길 추천해요", "짧은 코스로요", "무리하지 말아요"]],
      [38, ["가볍게만 걸어요", "정상은 다음에", "안전 위주로요"]],
      [0, ["실내가 나아요", "오늘은 쉬어가요", "다음 기회에요"]]
    ]
  }
```
ONE_LINERS.hike(rain/rainMaybe/dust/hot/uv/wind/humid/cold/good/ok/bad) — 두 줄 조언(등산·하산·능선 관점, 각 2개). BRIEFING_VERDICT.hike(5단, 각 4개) — 산행 판정 문구.
> 구현: 각 테이블에 hike 키를 위 톤으로 작성. composeBriefing의 caution/tip/generic·getMetricDetail의 isHike 분기도 추가(다음 Step).

- [ ] **Step 3: composeBriefing + getMetricDetail hike 분기**

`composeBriefing`: `const isHike = activity === "hike";` 추가. caution wind → hike면 "능선 강풍 주의", precip → "미끄럼 주의, 하산 여유". tip generic에 hike 키:
```ts
      hike: ["하산 시간 미리 계산", "물·간식 넉넉히", "무릎 보호 스틱", "정상 방한 한 겹", "일기예보 재확인", "헤드랜턴 챙기기"],
```
`getMetricDetail`: `METRIC_WORDS.hike = { doing: "산행", doingGood: "산행", hard: "긴 산행" }`, `const isHike = activity === "hike";`. 지표별 hike 분기:
- dust meaning: 조망 관점 추가("미세먼지가 많아 정상 조망이 아쉬워요").
- wind: "능선·정상은 바람이 훨씬 강해요. 저체온을 조심하세요."
- precip: "비 오면 바위·흙길이 미끄러워 하산이 특히 위험해요."
- feel: "정상부는 여기보다 6~10°C 낮아요. 겹쳐 입으세요."

- [ ] **Step 4: 검증 + Commit**

Run: `pnpm test && pnpm typecheck && pnpm build 2>&1 | tail -3`
```bash
git add lib/insights.ts
git commit -m "feat: 등산 콘텐츠 전면 + 새벽 포함 4시간대

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: outfit.ts — 등산 준비물

**Files:**
- Modify: `lib/outfit.ts`

- [ ] **Step 1: mainFor/topFor/bottomFor/headlineFor에 hike 분기**

`mainFor` hike: 레이어링 강조.
```ts
  if (activity === "hike") {
    if (feel >= 22) return "기능성 반팔 + 등산 반바지";
    if (feel >= 13) return "긴팔 + 바람막이 + 등산바지";
    if (feel >= 5) return "긴팔 + 플리스 + 바람막이";
    if (feel >= -3) return "보온 레이어 + 방풍 재킷";
    return "방한 등산복 풀레이어";
  }
```
`topFor`/`bottomFor`/`headlineFor` hike 분기도 등산 톤(정상 방한·레이어링)으로 추가. headlineFor hike:
```ts
  if (activity === "hike") {
    if (feel >= 24) return "더운 산행이에요. 이른 시간에 물 넉넉히, 그늘 코스를 노리세요.";
    if (feel >= 13) return "산행하기 좋은 온도예요. 정상 방한용 한 겹만 더 챙기세요.";
    if (feel >= 5) return "능선은 서늘해요. 바람막이와 플리스로 레이어링하세요.";
    if (feel >= -3) return "추운 산행이에요. 방풍·방한 단단히 하고 짧게 다녀오세요.";
    return "한파 산행이에요. 풀레이어와 아이젠, 무리는 금물이에요.";
  }
```

- [ ] **Step 2: getOutfitPlan hike 전용 준비물 카테고리 + getOutfit extras**

`getOutfitPlan`에 `const isHike = activity === "hike";` 추가하고 애견/자전거 블록 옆에:
```ts
  if (isHike) {
    categories.push({ emoji: "🎒", label: "필수 준비물", value: "물 + 간식 + 보조배터리", reason: "산에서는 체력·연락 수단이 안전이에요." });
    categories.push({ emoji: "🥾", label: "발", value: "등산화 + 스틱", reason: "미끄럼·무릎 보호에 큰 차이가 나요." });
    if (feel < 10 || current.windSpeed >= 8) {
      categories.push({ emoji: "🧥", label: "정상 방한", value: "바람막이·플리스 한 겹", reason: "정상부는 여기보다 훨씬 춥고 바람이 강해요." });
    }
  }
```
`getOutfit` extras에 `if (activity === "hike") extras.push("스틱");` 추가.

- [ ] **Step 3: 검증 + Commit**

Run: `pnpm typecheck && pnpm test`
```bash
git add lib/outfit.ts
git commit -m "feat: 등산 준비물·레이어링 복장

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 산 우선 검색 (route + 검색 UI 활동화)

**Files:**
- Modify: `app/api/search-location/route.ts`
- Modify: `app/page.tsx` (검색 타입·placeholder·정렬 — 일부는 Task 6과 함께)

**Interfaces:**
- Produces: `SearchResult`에 `kind?: "address"|"place"|"mountain"`, `categoryName?: string`. `/api/search-location?query=..&mountain=1` 시 산 우선 정렬.

- [ ] **Step 1: route SearchResult 확장 + 산 판정**

`app/api/search-location/route.ts`의 `SearchResult` 타입에 `kind?`, `categoryName?` 추가. `toResult`에 kind/categoryName 파라미터 추가. 키워드 검색(`searchKakao`)에서 `place_name`/`category_name`을 result에 담고, 산 판정 헬퍼:
```ts
function isMountain(name: string, category?: string): boolean {
  if (/(산|봉|악|오름|고개|령|대)$/.test(name.trim())) return true;
  if (category && /(산|공원|관광|명소|자연)/.test(category)) return true;
  return false;
}
```
GET에서 `const wantMountain = url.searchParams.get("mountain") === "1";` 읽고, 결과 정렬: wantMountain이면 `kind==="mountain"` 우선 → 그다음 키워드(place) → 주소.
```ts
  if (wantMountain) {
    results.sort((a, b) => (Number(b.kind === "mountain") - Number(a.kind === "mountain")));
  }
```

- [ ] **Step 2: page.tsx 검색 호출에 mountain 플래그 + placeholder**

`app/page.tsx`의 `SearchResult` 타입(로컬)에도 `kind?`, `categoryName?` 추가. 검색 fetch에 `activity === "hike"`면 `&mountain=1` 붙이기. 검색창 placeholder를 `activity === "hike" ? "산 이름 검색 (예: 북한산)" : "동네·지역 검색"`. 결과 표시에 categoryName/detail 활용해 "이름 · 지역".

- [ ] **Step 3: 검증**

Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
로컬 dev에서 카카오 키가 있으면 "북한산" 검색이 산 우선으로 뜨는지 확인(없으면 Nominatim 폴백이라 정렬만 확인).

- [ ] **Step 4: Commit**

```bash
git add app/api/search-location/route.ts app/page.tsx
git commit -m "feat: 등산 산 우선 검색 + 활동별 검색 문구

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: page.tsx — 등산 안전 카드 + tagline + 기본탭 + 시간대 연결

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: import + 기본탭 walk + getRankedWindows에 activity 전달**

`import { ACTIVITIES, ACTIVITY_ORDER, getDogPlan, getHikePlan, type ActivityKey } from "@/lib/activity";`
`useState<ActivityKey>("run")` → `useState<ActivityKey>("walk")`. localStorage 복원 조건에 `"hike"` 추가.
`getRankedWindows(slots, isToday, hour)` 호출부 2곳에 `, activity` 추가.

- [ ] **Step 2: hikePlan useMemo**

dogPlan useMemo 아래:
```tsx
const hikePlan = useMemo(() => {
  if (activity !== "hike" || !view || !forecast) return null;
  const ref = isTomorrow ? view.best : view.reference;
  return getHikePlan({
    hour: ref.hour, temperature: ref.temperature, apparentTemperature: ref.apparentTemperature,
    humidity: ref.humidity, windSpeed: ref.windSpeed, windGust: ref.windGust,
    precipitation: ref.precipitation, precipitationProbability: ref.precipitationProbability,
    uvIndex: ref.uvIndex, weatherCode: ref.weatherCode, visibility: ref.visibility,
    cloudCover: ref.cloudCover, snowfall: ref.snowfall, pm25: ref.pm25,
    sunrise: forecast.sunrise, sunset: forecast.sunset
  });
}, [activity, view, isTomorrow, forecast]);
```

- [ ] **Step 3: 등산 안전 카드 렌더 (dog-plan 렌더 옆)**

```tsx
{hikePlan ? (
  <section className="hike-plan" aria-label="등산 안내">
    {hikePlan.descentDeadline ? (
      <div className="hike-descent">
        <span aria-hidden="true">🌄</span>
        <div>
          <b>{hikePlan.sunsetText} · 하산 마감</b>
          <p>{hikePlan.descentDeadline}. {hikePlan.summitNote}</p>
        </div>
      </div>
    ) : null}
    {hikePlan.sunrisePlan ? <p className="hike-sunrise">🌅 {hikePlan.sunrisePlan}</p> : null}
    {hikePlan.signals.length > 0 ? (
      <ul className="hike-signals">
        {hikePlan.signals.map((s) => (
          <li key={s.text} className={`hs-${s.level}`}>
            <span aria-hidden="true">{s.emoji}</span> {s.text}
          </li>
        ))}
      </ul>
    ) : null}
    <p className="hike-view">👁️ {hikePlan.view}</p>
    <ul className="hike-check">
      {hikePlan.checklist.map((c) => (
        <li key={c}>{c}</li>
      ))}
    </ul>
  </section>
) : null}
```

- [ ] **Step 4: 활동 tagline 노출**

활동 탭(`<ActivityRail ... variant="tabs" />`) 바로 아래에:
```tsx
<p className="activity-tagline">{ACTIVITIES[activity].emoji} {ACTIVITIES[activity].tagline}</p>
```
드로어 활동 버튼에도 설명: `drawer-act` 버튼 내부에 `<small>{ACTIVITIES[key].tagline}</small>` 추가하고 버튼을 세로 정렬로.

- [ ] **Step 5: daypart 개수 대응**

dayparts-grid에 개수 클래스: `<div className={\`dayparts-grid cols-${view.parts.length}\`}>`. (4개일 때 CSS에서 2×2/4열 처리 — Task 7.)

- [ ] **Step 6: 검증 (브라우저)**

Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
preview로 등산 탭: 하산 마감·안전 신호·조망·준비물 카드, 새벽 포함 4시간대, tagline 노출 확인. 5개 활동 순회.

- [ ] **Step 7: 러닝 표현 누수 grep**

Run: `grep -n "러닝\|우중런\|뛰기" app/page.tsx | grep -v "러닝콜\|ACTIVITIES\|profile\.\|// \|러닝 관점"`
Expected: 사용자 노출 문자열에 러닝 전용 표현 없음.

- [ ] **Step 8: Commit**

```bash
git add app/page.tsx
git commit -m "feat: 등산 안전 카드 + 활동 설명(tagline) + 기본탭 걷기 + 새벽 시간대

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: globals.css — 등산 카드 + 4시간대 + tagline + 전 기기 반응형

**Files:**
- Modify: `app/globals.css`

- [ ] **Step 1: 등산 카드 스타일**

globals.css 끝에 추가:
```css
.hike-plan { margin-top: 12px; padding: 14px 16px; border-radius: 16px; background: var(--card); border: 1.5px solid var(--line); box-shadow: var(--shadow-sm); display: flex; flex-direction: column; gap: 10px; }
.hike-descent { display: flex; gap: 10px; align-items: flex-start; padding: 10px 12px; border-radius: 12px; background: #fff6ec; }
.hike-descent b { font-size: 0.92rem; font-weight: 800; color: #9a5a00; }
.hike-descent p { margin: 2px 0 0; font-size: 0.82rem; line-height: 1.5; color: var(--ink-2); }
.hike-sunrise { margin: 0; font-size: 0.85rem; font-weight: 600; color: var(--ink-2); }
.hike-signals { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 6px; }
.hike-signals li { font-size: 0.85rem; font-weight: 600; padding: 8px 11px; border-radius: 10px; background: #f7f9fc; }
.hike-signals .hs-danger { background: #fff3f3; color: var(--bad-deep); }
.hike-signals .hs-caution { background: #fff9ee; color: var(--warn-deep); }
.hike-view { margin: 0; font-size: 0.84rem; font-weight: 600; color: var(--ink-2); }
.hike-check { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; gap: 6px; }
.hike-check li { font-size: 0.8rem; font-weight: 700; color: var(--ink-2); background: #eef2f8; border-radius: 999px; padding: 5px 11px; }
```

- [ ] **Step 2: 활동 tagline + 드로어 설명 스타일**

```css
.activity-tagline { text-align: center; margin: -4px 0 10px; font-size: 0.82rem; font-weight: 600; color: var(--muted); }
.drawer-act { flex-direction: column; gap: 2px; align-items: center; }
.drawer-act small { font-size: 0.68rem; font-weight: 600; color: var(--muted); line-height: 1.3; }
```

- [ ] **Step 3: 4개 데이파트 반응형**

기존 `.dayparts-grid`(3열)에 개수 대응 추가:
```css
.dayparts-grid.cols-4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }
@media (max-width: 620px) {
  .dayparts-grid.cols-4 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}
```
(기존 3열 규칙은 유지; cols-3 기본.)

- [ ] **Step 4: 전 기기 반응형 보강**

작은 폰(≤360px) 히어로 점수·탭 라벨이 넘치지 않게:
```css
@media (max-width: 360px) {
  .activity-tabs .act-label { font-size: 0.78rem; }
  .activity-tabs .act-item { gap: 3px; padding: 9px 4px; }
  .gauge-num { font-size: 2.6rem; }
}
```
초대형 화면(≥1600px) 2단 폭이 과하게 벌어지지 않게 유지(이미 max 1060px). 태블릿 세로(768~1024)는 1단이므로 카드 폭 적정 — 확인만.

- [ ] **Step 5: 브라우저 검증 (전 기기)**

preview로 mobile(375)·small(360)·tablet(768)·iPad(1024)·desktop(1440)에서: 등산 카드·4시간대·tagline·검색 결과 오버플로우 0, 레이아웃 정상. 콘솔 에러 0.

- [ ] **Step 6: Commit**

```bash
git add app/globals.css
git commit -m "style: 등산 카드·4시간대·활동설명 + 전 기기 반응형 보강

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 최종 검증 + 문서 + v0.4.0 배포

**Files:**
- Modify: `package.json`, `CHANGELOG.md`, `docs/ROADMAP.md`, `docs/DEVELOPMENT_LOG.md`

- [ ] **Step 1: 전체 검증**

Run: `pnpm test && pnpm typecheck && rm -rf .next && pnpm build`
Expected: 골든마스터 포함 전 테스트 PASS, 빌드 성공.

- [ ] **Step 2: 전 활동·전 기기 육안 확인 (preview)**

5개 활동(걷기/등산/애견/러닝/자전거) 순회, 등산 산검색·안전카드·새벽시간대, 러닝 화면 불변. `grep -rn "commute\|출퇴근" app/ lib/` → 없음.

- [ ] **Step 3: 버전·문서**

- `package.json` `0.3.1` → `0.4.0`.
- `CHANGELOG.md` `[0.4.0]`: 등산 카테고리(산검색·등산점수·하산/일출/낙뢰/돌풍/조망/준비물·새벽시간대·날씨확장), 순서 변경, 문구/이모지/설명 정비, 전 기기 반응형.
- `ROADMAP.md`·`DEVELOPMENT_LOG.md` 갱신. v0.5.0(고도 정밀·주말산행·결빙 상세) 다음 타깃 표기.

- [ ] **Step 4: 커밋·태그·배포**

```bash
git add -A
git commit -m "release: v0.4.0 — 등산 카테고리 + 전면 업그레이드

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git tag -a v0.4.0 -m "v0.4.0 — 등산(산검색·하산안전·새벽시간대) + 순서/문구/반응형"
git push origin main && git push origin v0.4.0
```

- [ ] **Step 5: 배포 검증**

`curl -s -o /dev/null -w "%{http_code}" https://runningcall.vercel.app` → 200. 실배포에 등산 탭·산검색 확인.

---

## Self-Review

- [ ] 스펙 8개 결정 전부 대응: 순서(T2), hike프로필(T2), 날씨확장(T1), getHikePlan(T2), 새벽시간대(T3), 산검색(T5), tagline/문구(T2·T3·T6), 반응형(T7).
- [ ] 러닝 골든마스터로 불변 잠금(T1·T2). 새 날씨필드 slot 점수 미사용.
- [ ] 타입 일관: ActivityKey "hike", HikePlan/HikeSignal, HourlyInput optional 필드, DayPartKey "dawn" 태스크 간 일치.
- [ ] 카피 격리 grep(T6 Step7, T8 Step2).
- [ ] 4개 데이파트 UI 반응형(T6 Step5 + T7 Step3).
