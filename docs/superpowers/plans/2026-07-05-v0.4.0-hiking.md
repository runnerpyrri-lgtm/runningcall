# v0.4.0 등산 카테고리 + 전면 업그레이드 구현 계획 (최종)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 등산(hike) 카테고리를 산 검색·등산 점수·오늘/내일 하산 안전·04시 새벽 시간대·낙뢰/돌풍/조망 카드와 함께 추가하고, 앱 전체 문구를 야외활동 확장형으로 바꾸고, 카테고리별 오늘의 한마디를 수요 밀착으로 심화하고, 5개 탭 전 기기 반응형을 완성한다.

**Architecture:** 활동 프로필에 hike 추가. 날씨 fetch에 안전용 변수 + 오늘/내일 일출·일몰을 추가하되 slot 점수는 미사용(러닝 골든마스터 불변). getHikePlan이 안전 카드를, 활동별 분기가 시간대·검색·문구를 담당.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest, Open-Meteo, Kakao Local.

## Global Constraints
- **러닝 불변**: run 점수·톤·멘트 완전 불변. 새 날씨 필드 slot 미사용. `pnpm test` 골든마스터 통과 필수.
- 순서 `["walk", "dog", "run", "hike", "bike"]`, 기본 탭 walk.
- 카피 격리(활동 표현 안 샘), 한글 마침표 종결, 새 파일 한글 주석.
- 커밋 트레일러 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- PATH: `export PATH="/opt/homebrew/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"` + `cd "/Users/runner706/Developer/러닝콜"`.

---

## Task 1: weather.ts — 안전용 변수 + 오늘/내일 일출·일몰

**Files:** `lib/scoring.ts`, `lib/weather.ts`

**Interfaces:** HourlyInput optional `weatherCode?,windGust?,visibility?,cloudCover?,snowfall?`. RawForecast/RunningForecast에 `sunriseTomorrow`,`sunsetTomorrow` 추가.

- [ ] **Step 1: HourlyInput optional 필드 (scoring.ts)** — `weatherCode?,windGust?,visibility?,cloudCover?,snowfall?: number` 추가.
- [ ] **Step 2: weather.ts fetch 변수** — hourly에 `wind_gusts_10m,weather_code,visibility,cloud_cover,snowfall` 추가, `WeatherResponse.hourly` 타입·`allInputs` 매핑에 반영(visibility 기본 20000, 나머지 0).
- [ ] **Step 3: 오늘/내일 일출·일몰** — `RawForecast`·`RunningForecast` 타입에 `sunriseTomorrow: string|null; sunsetTomorrow: string|null` 추가. `fetchRawForecast`에서:
```ts
  const tomorrowDailyIndex = todayDailyIndex + 1;
  // return에 추가:
  sunriseTomorrow: weather.daily?.sunrise?.[tomorrowDailyIndex] ?? null,
  sunsetTomorrow: weather.daily?.sunset?.[tomorrowDailyIndex] ?? null
```
`scoreForecast`도 `sunriseTomorrow: raw.sunriseTomorrow, sunsetTomorrow: raw.sunsetTomorrow` 통과.
- [ ] **Step 4: 검증** — `pnpm test && pnpm typecheck` (골든마스터 통과).
- [ ] **Step 5: Commit** — `feat: 등산 안전 날씨변수 + 오늘/내일 일출일몰 분리`

---

## Task 2: activity.ts — hike 프로필 + 순서 + short + getHikePlan

**Files:** `lib/activity.ts`, `lib/scoring.ts`, `lib/__tests__/scoring.test.ts`

**Interfaces:** `ActivityKey` += `"hike"`. `ActivityProfile` += `short: string`. `ACTIVITY_ORDER=["walk","dog","run","hike","bike"]`. `getHikePlan`, `HikePlan`, `HikeSignal`.

- [ ] **Step 1: short 필드 + 순서 + 전 활동 tagline·short**

`ActivityProfile`에 `short: string` 추가. 각 활동:
- walk: short"걷기" tagline"가볍게 걷기 좋은 시간을 알려드려요"
- dog: short"산책" tagline"강아지 발바닥까지 생각한 산책 타이밍"
- run: short"러닝" tagline"달리기 좋은 순간을 짚어드려요"
- hike: short"등산" tagline"산 오르기 좋은 시간과 하산 여유까지"
- bike: short"자전거" tagline"바람까지 계산한 라이딩 타이밍"

`ACTIVITY_ORDER = ["walk", "dog", "run", "hike", "bike"];`

hike 프로필:
```ts
  hike: {
    key: "hike", label: "등산", short: "등산", emoji: "⛰️", tagline: "산 오르기 좋은 시간과 하산 여유까지",
    ready: true, resultMode: "score",
    weights: { dust: 0.12, temperature: 0.18, precipitation: 0.28, uv: 0.1, humidity: 0.1, wind: 0.22 },
    temp: { optimalLo: 6, optimalHi: 18, coldSlope: 4.5, hotSlope: 5 },
    heat: { hot1: 31, hot1Cap: 55, hot2: 34, hot2Cap: 38 },
    windCap: { speed: 12, cap: 38 },
    terms: { verbDo: "등산하", outfitTitle: "등산 준비물", alarmTitle: "산행 알림", rainyTag: "우중 산행", blockedTag: "산행 위험", nowTag: "지금 산행 딱!", notifReady: "산 오르기 좋은 시간이에요" }
  }
```

- [ ] **Step 2: getHikePlan (activity.ts 끝)**

날짜별 일출/일몰을 인자로 받음(page에서 오늘/내일 선택 후 전달).
```ts
export type HikeSignal = { level: "danger" | "caution"; emoji: string; text: string };
export type HikePlan = {
  sunsetText: string | null; descentDeadline: string | null; sunrisePlan: string | null;
  signals: HikeSignal[]; view: string; summitNote: string; checklist: string[];
};
function fmtAmPmKo(hour: number, minute: number) {
  const ap = hour < 12 ? "오전" : "오후"; const h = hour % 12 === 0 ? 12 : hour % 12;
  return `${ap} ${h}시${minute > 0 ? ` ${minute}분` : ""}`;
}
function parseClock(iso: string | null): { h: number; m: number } | null {
  if (!iso) return null; const h = Number(iso.slice(11, 13)); const m = Number(iso.slice(14, 16));
  if (!Number.isFinite(h)) return null; return { h, m: Number.isFinite(m) ? m : 0 };
}
export function getHikePlan(input: {
  hour: number; temperature: number; apparentTemperature: number; humidity: number; uvIndex: number;
  windSpeed: number; windGust?: number; precipitation: number; precipitationProbability: number;
  weatherCode?: number; visibility?: number; cloudCover?: number; snowfall?: number; pm25: number;
  sunrise: string | null; sunset: string | null;
}): HikePlan {
  const set = parseClock(input.sunset);
  let sunsetText: string | null = null, descentDeadline: string | null = null;
  if (set) {
    sunsetText = `일몰 ${fmtAmPmKo(set.h, set.m)}`;
    let dh = set.h - 2, dm = set.m - 30; if (dm < 0) { dm += 60; dh -= 1; }
    descentDeadline = `늦어도 ${fmtAmPmKo((dh + 24) % 24, dm)}엔 하산을 시작하세요`;
  }
  const rise = parseClock(input.sunrise);
  const sunrisePlan = rise && input.hour <= 8
    ? `일출 ${fmtAmPmKo(rise.h, rise.m)} · 정상 일출을 보려면 여유 있게 출발하세요` : null;

  const signals: HikeSignal[] = [];
  if (input.weatherCode !== undefined && input.weatherCode >= 95)
    signals.push({ level: "danger", emoji: "⛈️", text: "낙뢰 위험. 능선·정상은 매우 위험하니 산행을 미루세요." });
  const gust = input.windGust ?? input.windSpeed;
  if (gust >= 14) signals.push({ level: "danger", emoji: "💨", text: "돌풍이 강해요. 정상부 저체온·실족 위험이 커요." });
  else if (input.windSpeed >= 9) signals.push({ level: "caution", emoji: "🌬️", text: "능선 바람이 강해요. 정상부는 체감이 더 낮아요." });
  if (input.precipitation >= 0.5 || input.precipitationProbability >= 60)
    signals.push({ level: "caution", emoji: "🌧️", text: "비 소식이 있어요. 바위·흙길·계단 미끄럼을 조심하세요." });
  if ((input.snowfall ?? 0) > 0 || (input.temperature <= 0 && input.humidity >= 70))
    signals.push({ level: "caution", emoji: "❄️", text: "결빙·눈 가능. 아이젠과 스틱을 챙기세요." });

  const vis = input.visibility ?? 20000;
  const view = vis < 5000 || input.pm25 > 55 ? "오늘은 조망이 흐릴 수 있어요."
    : (input.cloudCover ?? 0) >= 80 ? "구름이 많아 조망은 아쉬울 수 있어요."
    : vis >= 15000 && input.pm25 <= 35 ? "조망이 트인 맑은 날이에요." : "조망은 무난한 편이에요.";
  const summitNote = "정상부는 기온보다 6~10°C 낮고 바람이 강해요. 겹쳐 입으세요.";

  const checklist = ["💧 충분한 물", "🧥 바람막이", "🔋 보조배터리"];
  if (input.uvIndex >= 5) checklist.push("🧢 모자·선크림");
  if (input.precipitation >= 0.3 || input.precipitationProbability >= 60) checklist.push("🌂 우비");
  if (input.hour >= 14 || (set && input.hour >= set.h - 3)) checklist.push("🔦 헤드랜턴");
  if ((input.snowfall ?? 0) > 0 || input.temperature <= 2) checklist.push("🧊 아이젠·장갑");

  return { sunsetText, descentDeadline, sunrisePlan, signals: signals.slice(0, 4), view, summitNote, checklist };
}
```

- [ ] **Step 3: COMMENTS.hike (scoring.ts)** — excellent/good/fair/caution/bad 각 4개, 등산 톤(산 오르기·조망·하산·안전).
- [ ] **Step 4: 테스트 추가 (scoring.test.ts)**
```ts
import { getHikePlan } from "@/lib/activity";
describe("등산", () => {
  const base = { time: "2026-04-01T13:00", temperature: 10, apparentTemperature: 9, humidity: 60, pm10: 30, pm25: 15, uvIndex: 3, precipitation: 0, precipitationProbability: 0, windSpeed: 2 };
  it("강수·강풍에 등산 점수가 크게 낮다", () => {
    const bad = { ...base, precipitation: 2, precipitationProbability: 90, windSpeed: 13 };
    expect(calculateSlot(base as any, ACTIVITIES.hike).totalScore - calculateSlot(bad as any, ACTIVITIES.hike).totalScore).toBeGreaterThanOrEqual(30);
  });
  it("getHikePlan이 낙뢰·하산마감을 낸다", () => {
    const p = getHikePlan({ hour: 12, temperature: 15, apparentTemperature: 15, humidity: 50, uvIndex: 4, windSpeed: 3, precipitation: 0, precipitationProbability: 0, weatherCode: 96, pm25: 20, sunrise: "2026-04-01T05:50", sunset: "2026-04-01T19:00" });
    expect(p.signals.some((s) => s.emoji === "⛈️")).toBe(true);
    expect(p.descentDeadline).toContain("하산");
  });
});
```
- [ ] **Step 5: 검증 + Commit** — `pnpm test && pnpm typecheck` → `feat: 등산 프로필·getHikePlan·순서(걷기→애견→러닝→등산→자전거)·short 라벨`

---

## Task 3: insights.ts — 등산 콘텐츠 + 04시 새벽 + 오늘의 한마디 심화

**Files:** `lib/insights.ts`

- [ ] **Step 1: 04~20시 시간대 (활동별)**
`getRankedWindows(slots, isToday, nowHour, activity="run")`: `const startH = activity === "hike" ? 4 : 6; const endH = activity === "hike" ? 18 : 22;` 루프 `for (let h = startH; h <= endH; h++)`.
`DayPartKey` += `"dawn"`. hike 4구간:
```ts
const HIKE_DAY_PARTS = [
  { key: "dawn", label: "새벽", icon: "sunrise", range: [4, 7] },
  { key: "morning", label: "아침", icon: "sunrise", range: [8, 10] },
  { key: "day", label: "낮", icon: "day", range: [11, 15] },
  { key: "evening", label: "저녁", icon: "sunset", range: [16, 19] }
] as const;
```
`getDayParts(slots, isToday, nowHour, activity="run")`: `const defs = activity === "hike" ? HIKE_DAY_PARTS : DAY_PARTS;`. `DAY_PART_DESC` 타입을 `Record<ActivityKey, string[]>`로, hike=`["새벽 산행","아침 산행","한낮 산행","저녁 산행"]`, 나머지 3개 유지. desc는 index로 매핑.

- [ ] **Step 2: HEADLINE_SCORE.hike / SUBLINES.hike / BRIEFING_VERDICT.hike / composeBriefing·getMetricDetail hike 분기** — 등산 톤(하산·능선·정상·조망). getMetricDetail: `isHike`, dust=조망 관점, wind=능선/정상, precip=하산 미끄럼, feel=정상 체감. METRIC_WORDS.hike=`{doing:"산행",doingGood:"산행",hard:"긴 산행"}`. composeBriefing generic.hike=`["하산 시간 미리 계산","물·간식 넉넉히","무릎 보호 스틱","정상 방한 한 겹","예보 재확인","헤드랜턴 챙기기"]`.

- [ ] **Step 3: 오늘의 한마디(ONE_LINERS) 심화 — 조건별 3~4개, 수요 밀착 (핵심)**

모든 활동의 ONE_LINERS를 **조건별 3~4개**로 확장하고, 활동 사용자가 진짜 필요로 하는 걸 담는다. **애견은 강아지를 아끼는 진심 + 실질 안전**을 최우선으로. 아래 애견 예시 수준의 밀도·톤으로 5개 활동 전부 작성.

애견(dog) 예시 (이 수준으로):
```ts
  dog: {
    good: [
      "오늘 같은 날은 강아지도 꼬리부터 흔들 거예요. 좋아하는 냄새 실컷 맡게 여유 있게 걸어주세요.",
      "산책하기 딱 좋아요. 평소보다 조금 더 걸으며 새 골목을 탐험시켜 주면 강아지가 정말 행복해해요.",
      "날씨가 순해요. 물 한 통 챙겨서 중간중간 쉬어가며 천천히 둘러보세요.",
      "강아지랑 눈 맞추며 걷기 좋은 날이에요. 서두르지 말고 강아지 속도에 맞춰 주세요."
    ],
    hot: [
      "지금 아스팔트는 발바닥에 화상이에요. 손등을 5초 대봐서 뜨거우면 해 지고 나가는 게 안전해요.",
      "더운 날 강아지는 사람보다 훨씬 빨리 지쳐요. 그늘길로 짧게 돌고, 헥헥거림이 심하면 바로 쉬어주세요.",
      "물을 꼭 챙기세요. 혀가 축 늘어지거나 침이 끈적이면 열사병 신호예요. 즉시 그늘로 데려가세요."
    ],
    cold: [
      "쌀쌀해요. 소형견·단모종이라면 옷을 입혀 주고, 발이 시리지 않게 짧고 굵게 다녀오세요.",
      "찬 바닥은 발바닥과 관절에 부담이에요. 해 있는 따뜻한 시간에 다녀오는 게 좋아요.",
      "추운 날 산책 후엔 발과 배를 닦아 주면 감기·피부염을 막을 수 있어요."
    ],
    rain: [
      "비 오는 날 산책은 배변 위주로 짧게만요. 다녀와서 발과 배를 잘 닦아 주세요.",
      "젖은 길은 강아지도 미끄러워요. 우비를 입히고 가까운 곳만 얼른 다녀오세요.",
      "천둥을 무서워하는 아이라면 오늘은 실내 놀이로 스트레스를 풀어 주세요."
    ]
    // dust/uv/wind/humid/rainMaybe/ok/bad도 같은 밀도로 3~4개씩 작성.
  }
```
walk/run/hike/bike도 각 조건 3~4개, 활동 수요 반영(걷기=부담없는 건강·기분, 러닝=페이스·부상예방, 등산=하산시간·정상·안전, 자전거=바람·비 안전·장비). composeOneLiner은 hike 분기 추가.

- [ ] **Step 4: daypart 테스트**
```ts
import { getDayParts } from "@/lib/insights";
it("등산 데이파트는 새벽 포함 4구간", () => {
  const parts = getDayParts([], false, 6, "hike");
  expect(parts.length).toBe(4);
  expect(parts[0].label).toBe("새벽");
});
```
- [ ] **Step 5: 검증 + Commit** — `pnpm test && pnpm typecheck && pnpm build` → `feat: 등산 콘텐츠·04시 새벽 시간대 + 오늘의 한마디 수요밀착 확장`

---

## Task 4: outfit.ts — 등산 준비물·레이어링
- [ ] **Step 1:** mainFor/topFor/bottomFor/headlineFor hike 분기(레이어링·정상 방한), getOutfitPlan hike 준비물(🎒물·간식·보조배터리 / 🥾등산화·스틱 / 🧥정상방한), getOutfit extras "스틱".
- [ ] **Step 2: 검증 + Commit** — `pnpm typecheck && pnpm test` → `feat: 등산 준비물·레이어링 복장`

---

## Task 5: 산 우선 검색 (route + 테스트)

**Files:** `app/api/search-location/route.ts`, `lib/__tests__/search.test.ts`(신규)

- [ ] **Step 1: isMountain export (대 제외) + kind/categoryName**
```ts
// `대` 제외 — 건국대/경희대 오판 방지. 카카오 category_name 병행.
export function isMountain(name: string, category?: string): boolean {
  if (/(산|봉|악|오름|고개|령)$/.test(name.trim())) return true;
  if (category && /(산|공원|관광|명소|자연)/.test(category)) return true;
  return false;
}
```
`SearchResult` += `kind?: "address"|"place"|"mountain"; categoryName?: string`. searchKakao 키워드 결과에 category_name·kind 채움, GET에 `mountain=1`이면 mountain 우선 정렬.
- [ ] **Step 2: 검색 테스트 (search.test.ts)**
```ts
import { describe, it, expect } from "vitest";
import { isMountain } from "@/app/api/search-location/route";
describe("isMountain", () => {
  it("산/봉/악/오름/고개/령은 산", () => { ["북한산","백운봉","관악","성산일출봉오름","미시령"].forEach((n)=>expect(isMountain(n)).toBe(true)); });
  it("대학은 산 아님", () => { expect(isMountain("건국대")).toBe(false); expect(isMountain("경희대")).toBe(false); });
  it("카테고리에 공원/관광이면 산", () => { expect(isMountain("무슨무슨공원", "여행 > 관광명소")).toBe(true); });
});
```
(vitest include에 `app/**/*.test.ts`도 추가하거나 test 파일을 `lib/__tests__/`에 두고 라우트에서 isMountain을 import. 경로 별칭 `@/app/...` 동작 확인.)
- [ ] **Step 3: 검증 + Commit** — `pnpm test && pnpm typecheck` → `feat: 산 검색 판정(isMountain, 대 제외) + 정렬 + 테스트`

---

## Task 6: 앱 전체 확장 문구 + 가이드 개편 + tip 제거

**Files:** `app/layout.tsx`, `app/manifest.ts`, `app/page.tsx`(문구), `lib/guide.ts`, `app/api/tip/route.ts`(삭제)

- [ ] **Step 1: 메타·매니페스트** — layout title/description "러닝콜 — 걷기·등산·산책·러닝·자전거, 나가기 좋은 시간". manifest description 확장(name 유지).
- [ ] **Step 2: 공유·알림·드로어 부제** — 공유 fallback·드로어 브랜드 부제를 "걷기·등산·산책·러닝·자전거"로. 알림 제목 "러닝콜" 유지(브랜드).
- [ ] **Step 3: guide.ts 개편** — 러닝 전용 토픽을 **활동별 점수 기준 / 등산 전 체크(하산 시간·날씨 위험 신호) / 더위·미세먼지 안전 / 복장 레이어링** 등 활동 공통+등산 중심으로 교체. (드로어 가이드 6종 유지하되 내용 활동 확장형.)
- [ ] **Step 4: tip route 제거** — `app/api/tip/route.ts` 삭제. page.tsx에서 이 API 호출·관련 UI 있으면 제거(현재 미사용이면 삭제만). `grep -rn "api/tip\|/tip" app/` 로 참조 확인 후 정리.
- [ ] **Step 5: 검증 + Commit** — `pnpm typecheck && pnpm build` → `feat: 앱 전체 야외활동 확장 문구 + 가이드 개편 + 미사용 tip 제거`

---

## Task 7: page.tsx — hike 카드 + tagline + 기본탭 + 짧은 라벨 + 검색

**Files:** `app/page.tsx`

- [ ] **Step 1:** import getHikePlan, 기본탭 `useState<ActivityKey>("walk")`, localStorage 복원에 "hike" 추가, getRankedWindows/getDayParts 호출에 `activity` 전달.
- [ ] **Step 2: hikePlan useMemo** — isTomorrow면 `forecast.sunriseTomorrow/sunsetTomorrow`, 아니면 `forecast.sunrise/sunset` 사용해 getHikePlan 호출.
- [ ] **Step 3: 등산 안전 카드 렌더** (dog-plan 옆) — 하산 마감·일출·안전신호·조망·준비물.
- [ ] **Step 4: tagline 노출** — 탭 아래 `<p className="activity-tagline">{ACTIVITIES[activity].emoji} {ACTIVITIES[activity].tagline}</p>`, 드로어 활동 버튼에 `<small>{tagline}</small>`.
- [ ] **Step 5: 짧은 탭 라벨** — ActivityRail 탭에서 `item.short` 사용(dog="산책"). 히어로/시트/드로어 제목은 `label`(애견산책) 유지.
- [ ] **Step 6: 검색 활동화** — hike면 `&mountain=1`, placeholder "산 이름 검색 (예: 북한산)", 결과 표시 지역 병기.
- [ ] **Step 7: daypart 개수 클래스** — `<div className={\`dayparts-grid cols-${view.parts.length}\`}>`.
- [ ] **Step 8: 검증 + grep** — `pnpm typecheck && pnpm build`; `grep -n "러닝\|우중런\|뛰기" app/page.tsx | grep -v "러닝콜\|ACTIVITIES\|profile\.\|// \|러닝 관점"` 누수 0.
- [ ] **Step 9: Commit** — `feat: 등산 안전카드·활동 설명·기본탭 걷기·짧은 라벨·산 검색 연결`

---

## Task 8: globals.css — hike 카드 + 4데이파트 + 5탭 + 전 기기 반응형

**Files:** `app/globals.css`

- [ ] **Step 1: hike 카드 스타일** — `.hike-plan/.hike-descent/.hike-sunrise/.hike-signals(.hs-danger/.hs-caution)/.hike-view/.hike-check`.
- [ ] **Step 2: tagline·드로어 설명** — `.activity-tagline`, `.drawer-act{flex-direction:column}` + `.drawer-act small`.
- [ ] **Step 3: 4데이파트** — `.dayparts-grid.cols-4{grid-template-columns:repeat(4,1fr)}` + `@media(max-width:620px){.cols-4{repeat(2,1fr)}}`.
- [ ] **Step 4: 5탭 전 기기** — 탭 max-width 상향(예 640px), `@media(max-width:380px){.act-label{font-size:.76rem} .act-item{gap:3px;padding:9px 4px}}`. 5개가 iPhone SE(360)~1440+에서 넘침 없게.
- [ ] **Step 5: 브라우저 검증(전 기기)** — preview로 360/375/768/1024/1440에서 5탭·등산카드·4데이파트·검색 오버플로우 0, 콘솔 에러 0.
- [ ] **Step 6: Commit** — `style: 등산 카드·4데이파트·5탭·전 기기 반응형`

---

## Task 9: 최종 검증 + 문서 + v0.4.0 배포
- [ ] **Step 1:** `pnpm test && pnpm typecheck && rm -rf .next && pnpm build` 전부 통과.
- [ ] **Step 2:** 5활동 preview 순회(걷기/애견/러닝/등산/자전거), 등산 산검색·오늘·내일 하산마감·새벽 4시간대·안전카드, 러닝 불변. `grep -rn "commute\|출퇴근\|api/tip" app/ lib/` 없음.
- [ ] **Step 3:** package.json `0.4.0`, CHANGELOG `[0.4.0]`(등산 풀세트·순서·확장문구·한마디심화·반응형), ROADMAP/LOG 갱신(v0.5.0 고도·주말산행 다음).
- [ ] **Step 4:** 커밋·태그 v0.4.0·push(자동배포).
- [ ] **Step 5:** `curl -s -o /dev/null -w "%{http_code}" https://runningcall.vercel.app` → 200.

---

## Self-Review
- [ ] R1 오늘/내일 일출일몰(T1·T2·T7), R2 04시·4데이파트(T3), R3 isMountain 대제외+테스트(T5), R4 확장문구(T6), R5 가이드·tip(T6), R6 5탭 반응형(T7·T8), R7 테스트(T2·T3·T5).
- [ ] 순서 ["walk","dog","run","hike","bike"] 기본 walk.
- [ ] 오늘의 한마디 조건별 3~4개·수요밀착(T3 Step3), 애견 진심+실용.
- [ ] 러닝 골든마스터 불변(T1·T2). 타입 일관(ActivityKey hike, HikePlan, short, sunriseTomorrow, DayPartKey dawn).
