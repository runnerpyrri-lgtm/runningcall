# v0.4.1 활동별 허브 구조 재설계 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 각 활동을 판단/준비/기록/가이드 내부 탭을 가진 허브로 만들고, PC 좌측 카테고리 레일을 살리고, 기록을 활동별로 분리(마이그레이션 포함)하고, 죽은 코드·누락 문구를 정리한다.

**Architecture:** page.tsx에 내부 탭 상태를 추가해 기존 메인 화면을 "판단" 탭으로 감싸고, 준비/기록/가이드 뷰를 새로 렌더한다. 기록은 `lib/activity-record.ts`의 활동별 저장 구조로 옮기고 기존 runlog를 run으로 마이그레이션한다. PC는 좌측 레일, 모바일은 상단 5탭.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest.

## Global Constraints
- **러닝 불변**: run 점수·톤·멘트 불변, `pnpm test` 골든마스터 통과 필수.
- **기록 무손실 마이그레이션**: 기존 `running-alarm:runlog` → run 배열.
- 순서 `["walk", "dog", "run", "hike", "bike"]`, 기본 탭 walk.
- 새 파일 첫 줄 한글 주석, 한글 마침표 종결.
- 커밋 트레일러 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- PATH: `export PATH="/opt/homebrew/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"` + `cd "/Users/runner706/Developer/러닝콜"`.

---

## File Structure
- **Create** `lib/activity-record.ts` — 활동별 기록 저장 shape·마이그레이션·목표.
- **Create** `lib/activity-guide.ts` — 활동별 실전 준비/가이드 콘텐츠.
- **Modify** `app/page.tsx` — 내부 탭·PC 레일·활동별 기록·공유 문구.
- **Modify** `app/globals.css` — 레일·세그먼트·기록/가이드·5탭 반응형.
- **Delete** `lib/tips.ts`. **Modify** `README.md`.
- **Create** `lib/__tests__/activity-record.test.ts`.

---

## Task 1: 정리 — tips.ts 삭제 + 공유/드로어/README 등산 포함

**Files:** delete `lib/tips.ts`, modify `app/page.tsx`, `README.md`

- [ ] **Step 1: tips.ts 삭제 확인** — `grep -rn "lib/tips\|from \"@/lib/tips\"" app/ lib/` 결과 없음 확인 후 `rm lib/tips.ts`.
- [ ] **Step 2: 공유 fallback + 드로어 부제 등산 포함** — `app/page.tsx`에서 두 문자열 교체:
  - `"러닝콜 — 러닝·걷기·산책·자전거, 나가기 좋은 시간"` → `"러닝콜 — 걷기·애견산책·러닝·등산·자전거, 나가기 좋은 시간"`
  - 드로어 `<small>러닝·걷기·산책·자전거, 나가기 좋은 시간</small>` → `<small>걷기·애견산책·러닝·등산·자전거, 나가기 좋은 시간</small>`
- [ ] **Step 3: README 수정** — `/api/tip` 언급 줄 삭제하고 활동 5종·주요 라우트(forecast/reverse-location/search-location)만 반영.
- [ ] **Step 4: 검증 + Commit**
  Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
  ```bash
  git add -A
  git commit -m "chore: tips.ts 죽은 코드 삭제 + 공유/드로어/README 등산 포함

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 2: `lib/activity-record.ts` — 활동별 기록 + 마이그레이션

**Files:** Create `lib/activity-record.ts`, Create `lib/__tests__/activity-record.test.ts`

**Interfaces:**
- Produces:
  - `type ActivityLog = Record<ActivityKey, string[]>`
  - `type GoalPeriod = "week" | "month"`; `type ActivityGoal = { count: number; period: GoalPeriod }`
  - `emptyLog(): ActivityLog`
  - `migrateLog(oldRunlog: string[] | null, existingNew: ActivityLog | null): ActivityLog`
  - `toggleDay(log: ActivityLog, activity: ActivityKey, date: string): ActivityLog`
  - `DEFAULT_GOALS: Record<ActivityKey, ActivityGoal>`
  - `recordButtonLabel(activity: ActivityKey): string`

- [ ] **Step 1: 실패 테스트 (마이그레이션·토글)**

`lib/__tests__/activity-record.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { emptyLog, migrateLog, toggleDay, DEFAULT_GOALS } from "@/lib/activity-record";

describe("activity-record", () => {
  it("빈 로그는 5개 활동 배열", () => {
    const log = emptyLog();
    expect(Object.keys(log).sort()).toEqual(["bike", "dog", "hike", "run", "walk"]);
    expect(log.run).toEqual([]);
  });

  it("기존 runlog를 run으로 마이그레이션", () => {
    const log = migrateLog(["2026-01-01", "2026-01-02"], null);
    expect(log.run).toEqual(["2026-01-01", "2026-01-02"]);
    expect(log.walk).toEqual([]);
  });

  it("신규 로그가 이미 있으면 마이그레이션하지 않는다", () => {
    const existing = { ...emptyLog(), walk: ["2026-02-02"] };
    const log = migrateLog(["2026-01-01"], existing);
    expect(log).toEqual(existing);
  });

  it("toggleDay는 해당 활동만 토글", () => {
    let log = emptyLog();
    log = toggleDay(log, "hike", "2026-03-03");
    expect(log.hike).toEqual(["2026-03-03"]);
    log = toggleDay(log, "hike", "2026-03-03");
    expect(log.hike).toEqual([]);
    expect(log.run).toEqual([]);
  });

  it("활동별 기본 목표 — 걷기 주5·애견 주7·러닝 주3·등산 월2·자전거 주2", () => {
    expect(DEFAULT_GOALS.walk).toEqual({ count: 5, period: "week" });
    expect(DEFAULT_GOALS.dog).toEqual({ count: 7, period: "week" });
    expect(DEFAULT_GOALS.run).toEqual({ count: 3, period: "week" });
    expect(DEFAULT_GOALS.hike).toEqual({ count: 2, period: "month" });
    expect(DEFAULT_GOALS.bike).toEqual({ count: 2, period: "week" });
  });
});
```

- [ ] **Step 2: 실행 — 실패 확인** — `pnpm test lib/__tests__/activity-record.test.ts` → 모듈 없음 실패.

- [ ] **Step 3: 구현**

```ts
// 활동별 운동 기록 저장 구조·마이그레이션·목표
import type { ActivityKey } from "@/lib/activity";

export type ActivityLog = Record<ActivityKey, string[]>;
export type GoalPeriod = "week" | "month";
export type ActivityGoal = { count: number; period: GoalPeriod };

export function emptyLog(): ActivityLog {
  return { walk: [], dog: [], run: [], hike: [], bike: [] };
}

// 기존 러닝 로그(running-alarm:runlog)를 run으로 옮긴다. 신규 로그가 이미 있으면 유지.
export function migrateLog(oldRunlog: string[] | null, existingNew: ActivityLog | null): ActivityLog {
  if (existingNew) return existingNew;
  const log = emptyLog();
  if (oldRunlog && Array.isArray(oldRunlog)) log.run = [...oldRunlog];
  return log;
}

export function toggleDay(log: ActivityLog, activity: ActivityKey, date: string): ActivityLog {
  const days = log[activity];
  const next = days.includes(date) ? days.filter((d) => d !== date) : [...days, date];
  return { ...log, [activity]: next };
}

export const DEFAULT_GOALS: Record<ActivityKey, ActivityGoal> = {
  walk: { count: 5, period: "week" },
  dog: { count: 7, period: "week" },
  run: { count: 3, period: "week" },
  hike: { count: 2, period: "month" },
  bike: { count: 2, period: "week" }
};

export function recordButtonLabel(activity: ActivityKey): string {
  switch (activity) {
    case "walk": return "오늘 걸었어요 🚶";
    case "dog": return "오늘 산책했어요 🐕";
    case "run": return "오늘 뛰었어요 🏃";
    case "hike": return "오늘 산행했어요 ⛰️";
    case "bike": return "오늘 탔어요 🚴";
  }
}
```

- [ ] **Step 4: 통과 확인 + Commit** — `pnpm test && pnpm typecheck`
  ```bash
  git add lib/activity-record.ts lib/__tests__/activity-record.test.ts
  git commit -m "feat: 활동별 기록 저장 구조 + runlog 마이그레이션 + 활동별 목표

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 3: `lib/activity-guide.ts` — 활동별 실전 준비/가이드

**Files:** Create `lib/activity-guide.ts`

**Interfaces:**
- Produces: `type GuideBlock = { heading: string; items: string[] }`; `ACTIVITY_GUIDE: Record<ActivityKey, { prep: GuideBlock[]; guide: GuideBlock[] }>`

- [ ] **Step 1: 구현** — 5개 활동 각각 `prep`(준비/주의)와 `guide`(실전 팁) 블록. 얕은 문구 금지. 예(등산):
```ts
// 활동별 실전 준비·가이드 콘텐츠 (준비/가이드 내부 탭에서 사용)
import type { ActivityKey } from "@/lib/activity";
export type GuideBlock = { heading: string; items: string[] };

export const ACTIVITY_GUIDE: Record<ActivityKey, { prep: GuideBlock[]; guide: GuideBlock[] }> = {
  hike: {
    prep: [
      { heading: "🎒 필수 준비물", items: ["물은 평소의 1.5배", "간식·행동식(저혈당 대비)", "보조배터리·헤드랜턴", "스틱(무릎 보호)", "여벌 상의(땀 젖음 대비)"] },
      { heading: "⚠️ 출발 전 확인", items: ["하산 시각 = 일몰 2시간 30분 전", "낙뢰·돌풍 예보 있으면 미루기", "정상은 평지보다 6~10°C 낮음"] }
    ],
    guide: [
      { heading: "🌄 하산이 안전의 8할", items: ["오르막보다 내리막에서 사고가 많아요", "무릎은 스틱으로, 속도는 절반으로", "어두워지기 전에 반드시 하산 시작"] },
      { heading: "🌅 일출 산행", items: ["정상 도착 역산해 출발 시각 잡기", "헤드랜턴·여벌 배터리 필수", "정상 바람은 예상보다 강하고 추워요"] },
      { heading: "⛈️ 날씨 위험 신호", items: ["능선에서 천둥 → 즉시 낮은 곳으로", "돌풍엔 바위 구간 우회", "안개 짙으면 갈림길에서 길 잃기 쉬움"] }
    ]
  },
  // walk / dog / run / bike 도 같은 밀도로 prep·guide 작성 (스펙의 활동별 항목 반영)
};
```
walk(미세먼지·자외선·오래걷기·출퇴근), dog(발바닥·지면열·배변산책·노령견·소형견), run(페이스·습도·워밍업·회복·조깅/인터벌), bike(맞바람·측풍·젖은노면·라이트·헬멧·강변길) 전부 prep 2블록 + guide 2~3블록으로.

- [ ] **Step 2: 검증 + Commit** — `pnpm typecheck`
  ```bash
  git add lib/activity-guide.ts
  git commit -m "feat: 활동별 실전 준비·가이드 콘텐츠

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 4: page.tsx — 활동별 기록 상태 전환 (기존 runLog → activity-log)

**Files:** Modify `app/page.tsx`

- [ ] **Step 1: import + state 교체** — `import { emptyLog, migrateLog, toggleDay, DEFAULT_GOALS, recordButtonLabel, type ActivityLog } from "@/lib/activity-record";`. `runLog`/`weeklyGoal` 상태를 `const [activityLog, setActivityLog] = useState<ActivityLog>(emptyLog());` + `const [goals, setGoals] = useState(DEFAULT_GOALS);`로.
- [ ] **Step 2: 복원·저장·마이그레이션 useEffect** — 마운트 시 신규 키(`running-alarm:activity-log:v1`) 읽고, 없으면 기존 `running-alarm:runlog` 읽어 `migrateLog`로 만들어 저장. 목표는 `running-alarm:activity-goals:v1`. 변경 시 저장.
- [ ] **Step 3: 파생값** — `const runSet = useMemo(() => new Set(activityLog[activity]), [activityLog, activity]);` (현재 활동 기준). `toggleRunDay(date)` → `setActivityLog((prev) => toggleDay(prev, activity, date))`.
- [ ] **Step 4: RecordSheet props 활동화** — 기록 계산이 현재 활동 set으로. 목표 표시는 `goals[activity]`(period 반영: month면 monthCount, week면 weekCount). (RecordSheet는 Task 5의 기록 탭으로 이전되지만, 우선 데이터 배선.)
- [ ] **Step 5: 검증** — `pnpm typecheck && pnpm build`. 기록 토글이 활동별로 분리되는지 preview 확인.
- [ ] **Step 6: Commit** — `feat: 기록을 활동별(activity-log)로 전환 + 마이그레이션 배선`

---

## Task 5: page.tsx — 내부 탭(판단/준비/기록/가이드)

**Files:** Modify `app/page.tsx`

- [ ] **Step 1: 내부 탭 상태** — `const [innerTab, setInnerTab] = useState<"today" | "prep" | "record" | "guide">("today");`. `changeActivity`에서 `setInnerTab("today")` 추가(활동 전환 시 리셋).
- [ ] **Step 2: 세그먼트 컨트롤 렌더** — 활동 tagline 아래에:
```tsx
<nav className="inner-tabs" aria-label="세부 보기">
  {([["today","오늘 판단"],["prep","준비"],["record","기록"],["guide","가이드"]] as const).map(([k, label]) => (
    <button key={k} type="button" className={`inner-tab${innerTab === k ? " on" : ""}`} aria-pressed={innerTab === k} onClick={() => setInnerTab(k)}>
      {label}
    </button>
  ))}
</nav>
```
- [ ] **Step 3: 판단 탭 래핑** — 기존 `<div className="day-toggle">`부터 지표(reasons) 섹션까지를 `{innerTab === "today" && (<>...</>)}`로 감싼다. (content-cols 구조는 판단 탭 안에 유지.)
- [ ] **Step 4: 준비/기록/가이드 뷰 렌더** — 판단 블록 뒤에:
```tsx
{innerTab === "prep" && view ? <PrepView slot={view.reference} slots={view.slots} activity={activity} /> : null}
{innerTab === "record" ? <RecordView activity={activity} log={activityLog} goal={goals[activity]} onToggle={toggleRunDay} /> : null}
{innerTab === "guide" ? <GuideView activity={activity} /> : null}
```
- [ ] **Step 5: PrepView/RecordView/GuideView 컴포넌트 작성** — PrepView는 `getOutfitPlan(slots, slot, activity)` 렌더(기존 OutfitSheet 본문 재사용)로 복장·준비물·주의 표시 + `ACTIVITY_GUIDE[activity].prep`. RecordView는 기존 RecordSheet 달력·스트릭·목표를 인라인화(현재 활동 log 기준, recordButtonLabel 사용). GuideView는 `ACTIVITY_GUIDE[activity].guide` 블록 렌더.
- [ ] **Step 6: 드로어 기록 진입 정리** — 드로어 "나의 활동 기록" 클릭 → `setInnerTab("record")` + 드로어 닫기 (별도 시트 대신 기록 탭). 기존 RecordSheet 시트가 남으면 제거하거나 기록 탭으로 통합.
- [ ] **Step 7: 검증** — `pnpm typecheck && pnpm build`. preview로 4개 내부 탭 전환·활동별 콘텐츠·기록 분리 확인.
- [ ] **Step 8: Commit** — `feat: 활동 내부 탭(판단/준비/기록/가이드) + PrepView/RecordView/GuideView`

---

## Task 6: page.tsx + CSS — PC 좌측 레일

**Files:** Modify `app/page.tsx`, `app/globals.css`

- [ ] **Step 1: 레일 렌더** — `.dashboard-frame` 안, `app-shell` 앞에 `<ActivityRail activity={activity} onChange={changeActivity} variant="rail" />` 추가. rail 컴포넌트는 이모지+전체 라벨+tagline+선택상태 표시하도록 보강:
```tsx
// ActivityRail rail 분기 내부 버튼
<span className="act-emoji">{item.emoji}</span>
<span className="act-rail-text"><strong>{item.label}</strong><small>{item.tagline}</small></span>
```
- [ ] **Step 2: 레이아웃 CSS** — 데스크탑에서 레일 노출·프레임 2단:
```css
.activity-rail { display: none; }
@media (min-width: 1200px) {
  .dashboard-frame { grid-template-columns: 210px minmax(0, 1fr); max-width: 1320px; column-gap: 24px; }
  .activity-rail { display: flex; flex-direction: column; gap: 8px; position: sticky; top: 22px; background: var(--card); border: 1px solid var(--line); border-radius: 18px; padding: 14px 12px; box-shadow: var(--shadow-sm); }
  .activity-tabs { display: none; }   /* PC는 레일로 대체 */
}
.activity-rail .act-item { display: flex; align-items: center; gap: 10px; padding: 11px 12px; border-radius: 14px; border: 1.5px solid transparent; background: #fff; text-align: left; }
.activity-rail .act-item.on { border-color: var(--brand); background: var(--brand-soft); }
.act-rail-text { display: flex; flex-direction: column; min-width: 0; }
.act-rail-text strong { font-size: 0.9rem; font-weight: 800; }
.act-rail-text small { font-size: 0.68rem; color: var(--muted); line-height: 1.25; }
```
- [ ] **Step 3: 브라우저 검증** — 1200/1440/1920px에서 레일 노출·선택 동작·오버플로우 0. 1080~1199px·모바일은 상단 탭.
- [ ] **Step 4: Commit** — `feat: PC 좌측 카테고리 레일(이모지·이름·설명·선택상태)`

---

## Task 7: CSS — 내부 세그먼트·기록/가이드·5탭 정리·전 기기 반응형

**Files:** Modify `app/globals.css`

- [ ] **Step 1: 내부 세그먼트** — `.inner-tabs`(4개 균등 pill, sticky 아님), `.inner-tab`/`.on`.
- [ ] **Step 2: 준비/가이드/기록 인라인 스타일** — `.guide-block` 헤딩+리스트, PrepView·RecordView 인라인 카드.
- [ ] **Step 3: 5탭 모바일 정리** — 활성 pill 또렷, 라벨 잘림 없이 균등. 360/375/430 확인.
- [ ] **Step 4: 브라우저 검증(전 기기)** — 360/375/430/768/1024/1280/1440/1920에서 5탭(모바일)·레일(PC)·내부탭·기록·가이드 오버플로우 0, 콘솔 에러 0.
- [ ] **Step 5: Commit** — `style: 내부 세그먼트·기록/가이드·5탭·전 기기 반응형`

---

## Task 8: 테스트 확장 + 문서 + v0.4.1 배포

**Files:** `lib/__tests__/`, `package.json`, `CHANGELOG.md`, `docs/*`

- [ ] **Step 1: 순서·활동별 스트릭 테스트** — `ACTIVITY_ORDER`가 `["walk","dog","run","hike","bike"]`인지, 활동별 log의 currentStreak/weekCount가 다른 활동과 독립인지 테스트 추가.
- [ ] **Step 2: 전체 검증** — `pnpm test && pnpm typecheck && rm -rf .next && pnpm build` 전부 통과(골든마스터 포함).
- [ ] **Step 3: 문서** — package.json `0.4.1`, CHANGELOG `[0.4.1]`(허브 구조·PC 레일·내부 탭·활동별 기록·실전 콘텐츠·정리), ROADMAP/LOG 갱신.
- [ ] **Step 4: 커밋·태그·배포** — `release: v0.4.1`, tag `v0.4.1`, push(자동배포).
- [ ] **Step 5: 배포 검증** — `curl -s -o /dev/null -w "%{http_code}" https://runningcall.vercel.app` → 200. 5활동·내부탭·PC 레일 확인.

---

## Self-Review
- [ ] 스펙 대응: 순서(현행), PC 레일(T6), 내부 탭(T5), 활동별 기록+마이그레이션(T2·T4), 실전 콘텐츠(T3·T5), 정리(T1), 5탭·반응형(T7), 테스트(T2·T8).
- [ ] 러닝 골든마스터 불변(T8). 기록 마이그레이션 무손실(T2 테스트).
- [ ] 타입 일관: ActivityLog·ActivityGoal·GuideBlock·innerTab 유니온 태스크 간 일치.
- [ ] 죽은 코드: tips.ts 삭제(T1), rail은 되살림(T6).
