# v0.3.0 (자전거 교체 · 텍스트/색상 정비 · 애견 강화) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 출퇴근을 자전거로 교체하고, 공용 컴포넌트의 러닝 표현을 전부 활동별로 바꾸고, 1등/일출 카드 색 충돌을 없애고, 애견 전용 정보 카드를 추가하고, 카테고리 선택을 상단 탭으로 통일한다.

**Architecture:** 활동별 UI 문구를 `ActivityProfile.terms`에 모아 공용 컴포넌트가 참조하게 하고(하드코딩 제거), 자전거 프로필은 바람 가중·강풍 위험캡을 새로 준다. 애견은 `getDogPlan`으로 신호등·산책길이·체크리스트를 계산한다. 러닝은 골든마스터로 계속 불변.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest, Open-Meteo API.

## Global Constraints

- **러닝 불변**: run 프로필 점수·톤·멘트 완전 불변. `pnpm test`의 골든마스터 통과 못 하면 배포 금지.
- **카피 격리**: 각 활동 화면에 다른 활동 표현("우중런", "러닝 복장" 등)이 새면 안 됨. 완료 후 grep 검증.
- **데이터 보존**: localStorage 키(`running-alarm:*`) 유지. 마지막 선택 활동 기억 유지.
- **새 파일 첫 줄**: 한글 역할 주석. **한글 문장**: 마침표로 끝, 콜론 금지.
- **커밋 트레일러**: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **PATH**: `export PATH="/opt/homebrew/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"` + `cd "/Users/runner706/Developer/러닝콜"`.
- 활동 순서: `["run", "walk", "dog", "bike"]`. commute 완전 제거.

---

## File Structure

- **Modify** `lib/activity.ts` — commute 제거 + bike 추가, `terms` 필드, `windCap` 필드, `getDogPlan` 추가, `getUmbrellaAdvice` 제거, `getPawRisk` 유지.
- **Modify** `lib/scoring.ts` — applyCaps에 windCap 반영, COMMENTS commute→bike.
- **Modify** `lib/insights.ts` — 모든 활동 테이블 commute→bike, DAY_PART_DESC bike, rainy/blocked는 호출부에서 terms 사용(여긴 데이터만).
- **Modify** `lib/outfit.ts` — commute→bike 복장.
- **Modify** `lib/__tests__/scoring.test.ts` — commute 케이스 → bike 케이스.
- **Modify** `app/page.tsx` — 좌측 레일 제거, 텍스트 audit(terms), 애견 모듈, 자전거 강풍 안내, 우산 안내 제거.
- **Modify** `app/globals.css` — rank 카드 액센트바 재설계, 좌측 레일 CSS 제거, 상단 탭 데스크탑 표시, 애견 모듈 스타일.

---

## Task 1: activity.ts — 자전거 교체 + terms + windCap

**Files:**
- Modify: `lib/activity.ts`
- Test: `lib/__tests__/scoring.test.ts` (Task 2에서 함께)

**Interfaces:**
- Produces:
  - `type ActivityKey = "run" | "walk" | "dog" | "bike"`
  - `ActivityProfile`에 `terms: ActivityTerms`, `windCap: { speed: number; cap: number }` 추가
  - `type ActivityTerms = { verbDo: string; outfitTitle: string; alarmTitle: string; rainyTag: string; blockedTag: string; nowTag: string; notifReady: string }`
  - `ACTIVITIES` 4종(run/walk/dog/bike), `ACTIVITY_ORDER = ["run","walk","dog","bike"]`
  - `getPawRisk` 유지, `getUmbrellaAdvice` 삭제

- [ ] **Step 1: 타입에 terms·windCap 추가**

`ActivityProfile` 타입에 필드 추가:
```ts
export type ActivityTerms = {
  verbDo: string;      // "뛰" "걷" "산책하" "라이딩하"
  outfitTitle: string; // "러닝 복장" 등
  alarmTitle: string;  // "러닝 알림" 등
  rainyTag: string;    // "우중런" "우중 걷기" "비 산책" "빗속 라이딩"
  blockedTag: string;  // "러닝 어려움" 등
  nowTag: string;      // "지금 뛰기 딱!" 등
  notifReady: string;  // "러닝하기 좋은 시간이에요" 등
};

export type ActivityProfile = {
  key: ActivityKey;
  label: string;
  emoji: string;
  tagline: string;
  ready: boolean;
  resultMode: "score" | "advisory";
  weights: ActivityWeights;
  temp: TempCurve;
  heat: HeatCaps;
  windCap: { speed: number; cap: number };
  terms: ActivityTerms;
};
```
그리고 `ActivityKey`의 `"commute"`를 `"bike"`로 교체.

- [ ] **Step 2: ACTIVITIES 4종 재작성**

run/walk/dog에 `windCap: { speed: 14, cap: 40 }`(기존 하드캡과 동일)와 `terms` 추가, commute 블록을 bike로 교체:
```ts
export const ACTIVITIES: Record<ActivityKey, ActivityProfile> = {
  run: {
    key: "run", label: "러닝", emoji: "🏃", tagline: "달리기 좋은 순간을 짚어드려요",
    ready: true, resultMode: "score",
    weights: { dust: 0.28, temperature: 0.22, precipitation: 0.16, uv: 0.12, humidity: 0.11, wind: 0.11 },
    temp: { optimalLo: 8, optimalHi: 16, coldSlope: 6, hotSlope: 5.2 },
    heat: { hot1: 30, hot1Cap: 55, hot2: 33, hot2Cap: 35 },
    windCap: { speed: 14, cap: 40 },
    terms: { verbDo: "뛰", outfitTitle: "러닝 복장", alarmTitle: "러닝 알림", rainyTag: "우중런", blockedTag: "러닝 어려움", nowTag: "지금 뛰기 딱!", notifReady: "러닝하기 좋은 시간이에요" }
  },
  walk: {
    key: "walk", label: "걷기", emoji: "🚶", tagline: "가볍게 걷기 좋은 시간을 알려드려요",
    ready: true, resultMode: "score",
    weights: { dust: 0.3, temperature: 0.2, precipitation: 0.16, uv: 0.16, humidity: 0.07, wind: 0.11 },
    temp: { optimalLo: 10, optimalHi: 22, coldSlope: 5, hotSlope: 4 },
    heat: { hot1: 33, hot1Cap: 60, hot2: 36, hot2Cap: 40 },
    windCap: { speed: 14, cap: 40 },
    terms: { verbDo: "걷", outfitTitle: "걷기 복장", alarmTitle: "걷기 알림", rainyTag: "우중 걷기", blockedTag: "걷기 어려움", nowTag: "지금 걷기 딱!", notifReady: "걷기 좋은 시간이에요" }
  },
  dog: {
    key: "dog", label: "애견산책", emoji: "🐕", tagline: "강아지 발바닥까지 생각한 산책 타이밍",
    ready: true, resultMode: "score",
    weights: { dust: 0.32, temperature: 0.2, precipitation: 0.16, uv: 0.16, humidity: 0.06, wind: 0.1 },
    temp: { optimalLo: 8, optimalHi: 20, coldSlope: 5, hotSlope: 4.6 },
    heat: { hot1: 28, hot1Cap: 55, hot2: 31, hot2Cap: 35 },
    windCap: { speed: 14, cap: 40 },
    terms: { verbDo: "산책하", outfitTitle: "산책 준비물", alarmTitle: "산책 알림", rainyTag: "비 산책", blockedTag: "산책 자제", nowTag: "지금 산책 딱!", notifReady: "산책하기 좋은 시간이에요" }
  },
  bike: {
    key: "bike", label: "자전거", emoji: "🚴", tagline: "바람까지 계산한 라이딩 타이밍",
    ready: true, resultMode: "score",
    weights: { dust: 0.16, temperature: 0.18, precipitation: 0.24, uv: 0.12, humidity: 0.08, wind: 0.22 },
    temp: { optimalLo: 12, optimalHi: 24, coldSlope: 6, hotSlope: 4 },
    heat: { hot1: 32, hot1Cap: 60, hot2: 35, hot2Cap: 40 },
    windCap: { speed: 11, cap: 38 },
    terms: { verbDo: "라이딩하", outfitTitle: "라이딩 복장", alarmTitle: "라이딩 알림", rainyTag: "빗속 라이딩", blockedTag: "라이딩 어려움", nowTag: "지금 라이딩 딱!", notifReady: "라이딩하기 좋은 시간이에요" }
  }
};

export const ACTIVITY_ORDER: ActivityKey[] = ["run", "walk", "dog", "bike"];
```
weights 합 확인: bike = 0.16+0.18+0.24+0.12+0.08+0.22 = 1.00.

- [ ] **Step 3: getUmbrellaAdvice 삭제, getPawRisk 유지, getDogPlan 추가**

`getUmbrellaAdvice`와 `UmbrellaAdvice` 타입 삭제. `getPawRisk`는 그대로. 파일 끝에 애견 종합 플랜 추가:
```ts
export type DogPlan = {
  signal: "go" | "caution" | "avoid";
  signalText: string;
  walkLength: string;   // "짧게 (10분 이내)" 등
  paw: PawRisk;
  checklist: string[];
};

export function getDogPlan(input: {
  score: number; hour: number; temperature: number; apparentTemperature: number;
  uvIndex: number; precipitation: number; precipitationProbability: number;
}): DogPlan {
  const paw = getPawRisk({ hour: input.hour, temperature: input.temperature, uvIndex: input.uvIndex });

  let signal: DogPlan["signal"];
  if (input.score >= 60 && paw.level !== "danger") signal = "go";
  else if (input.score >= 40 && paw.level !== "danger") signal = "caution";
  else signal = "avoid";

  const signalText = signal === "go" ? "산책 가능" : signal === "caution" ? "짧게 주의" : "산책 자제";

  const walkLength =
    signal === "avoid"
      ? "배변만 짧게 (5분 이내)"
      : input.score >= 75 && paw.level === "safe"
      ? "길게 걸어도 좋아요 (40분+)"
      : input.score >= 55
      ? "보통 산책 (20~30분)"
      : "짧게 (10분 이내)";

  const checklist: string[] = ["💧 물", "🛍️ 배변봉투"];
  if (input.apparentTemperature >= 25) checklist.push("🌳 그늘길 코스");
  if (input.apparentTemperature <= 3) checklist.push("🧥 강아지 옷");
  if (input.precipitation >= 0.3 || input.precipitationProbability >= 60) checklist.push("🧻 발 닦을 수건");
  if (paw.level !== "safe") checklist.push("🐾 지면 온도 확인");

  return { signal, signalText, walkLength, paw, checklist };
}
```

- [ ] **Step 4: 타입 체크** (scoring 참조 깨짐은 Task 2에서 해결되므로 여기선 activity.ts 자체 문법만)

Run: `pnpm typecheck 2>&1 | head -20`
Expected: activity.ts 문법 에러 없음. (scoring.ts/insights.ts 등에서 commute 참조·terms 미사용 에러가 뜨는 건 정상 — Task 2~5에서 해소.)

> 커밋은 Task 2 이후 스코어링까지 통과할 때 함께.

---

## Task 2: scoring.ts — windCap 반영 + bike 멘트, 골든마스터 유지

**Files:**
- Modify: `lib/scoring.ts`
- Modify: `lib/__tests__/scoring.test.ts`

**Interfaces:**
- Consumes: `ACTIVITIES`, `ActivityKey`, `HeatCaps`, `TempCurve`, profile.windCap.
- Produces: `applyCaps(total, input, heat, windCap)` — 시그니처 확장(내부). `COMMENTS`에 bike 키.

- [ ] **Step 1: 테스트를 bike로 교체 (실패 예상)**

`lib/__tests__/scoring.test.ts`의 "활동별 점수 차이" describe에서 commute 테스트를 bike로 교체:
```ts
  it("강풍일 때 자전거 점수가 러닝보다 크게 낮다 (바람 위험)", () => {
    const windy: HourlyInput = { time: "2026-04-01T15:00", temperature: 15, apparentTemperature: 14, humidity: 50, pm10: 30, pm25: 15, uvIndex: 3, precipitation: 0, precipitationProbability: 10, windSpeed: 12 };
    const run = calculateSlot(windy, ACTIVITIES.run).totalScore;
    const bike = calculateSlot(windy, ACTIVITIES.bike).totalScore;
    expect(bike).toBeLessThan(run);
  });
```
(기존 "비 오는 날 출퇴근" 테스트 삭제. "더운 날 걷기 > 러닝", "애견 < 걷기"는 유지.)

- [ ] **Step 2: 테스트 실행 — bike 미정의로 실패 확인**

Run: `pnpm test 2>&1 | tail -8`
Expected: 골든마스터는 통과, bike 관련은 아직 통과/실패 혼재. (bike 프로필은 Task 1에서 이미 존재하므로 통과할 수도 있음. windCap 미반영이면 강풍 차이가 작아 실패할 수 있음 — 그게 Step 3 동기.)

- [ ] **Step 3: applyCaps에 windCap 반영**

`applyCaps`의 하드코딩 `if (input.windSpeed >= 14) cap = Math.min(cap, 40);`를 프로필 windCap으로:
```ts
function applyCaps(total: number, input: HourlyInput, heat: HeatCaps = ACTIVITIES.run.heat, windCap = ACTIVITIES.run.windCap) {
  let cap = 100;
  if (input.precipitation >= 4) cap = Math.min(cap, 25);
  else if (input.precipitation >= 1) cap = Math.min(cap, 45);
  else if (input.precipitationProbability >= 80) cap = Math.min(cap, 60);
  if (input.apparentTemperature >= heat.hot2) cap = Math.min(cap, heat.hot2Cap);
  else if (input.apparentTemperature >= heat.hot1) cap = Math.min(cap, heat.hot1Cap);
  if (input.apparentTemperature <= -10) cap = Math.min(cap, 35);
  else if (input.apparentTemperature <= -5) cap = Math.min(cap, 55);
  if (input.pm25 > 75 || input.pm10 > 150) cap = Math.min(cap, 30);
  if (input.windSpeed >= windCap.speed) cap = Math.min(cap, windCap.cap);
  if (input.uvIndex >= 11) cap = Math.min(cap, 50);
  return Math.min(total, cap);
}
```
그리고 `calculateSlot` 안의 호출을 `applyCaps(weighted, input, profile.heat, profile.windCap)`로 변경.

- [ ] **Step 4: COMMENTS commute→bike**

`COMMENTS`의 `commute` 키를 삭제하고 `bike` 추가:
```ts
  bike: {
    excellent: ["라이딩하기 최고예요", "완벽한 라이딩 날씨예요", "바람 없이 시원하게 달려요", "지금 페달 밟기 딱이에요"],
    good: ["라이딩하기 좋아요", "가볍게 달리기 좋아요", "기분 좋게 탈 수 있어요", "부담 없이 나가기 좋아요"],
    fair: ["무난한 라이딩이에요", "짧은 코스는 괜찮아요", "바람 보며 달려봐요", "가볍게라면 좋아요"],
    caution: ["짧게만 타는 게 좋아요", "바람·노면 주의해요", "무리한 코스는 피해요", "가까운 길만 돌아요"],
    bad: ["오늘 라이딩은 위험해요", "실내 운동이 나아요", "바람·비가 부담이에요", "쉬어가는 날이에요"]
  }
```

- [ ] **Step 5: 테스트·타입·빌드**

Run: `pnpm test && pnpm typecheck`
Expected: 골든마스터 2개 + 걷기/애견/자전거 차이 테스트 모두 PASS.

- [ ] **Step 6: Commit** (Task 1 + 2 함께)

```bash
git add lib/activity.ts lib/scoring.ts lib/__tests__/scoring.test.ts
git commit -m "feat: 출퇴근→자전거 교체 + terms/windCap + getDogPlan

바람 가중·강풍 위험캡, 활동별 UI 문구 terms, 애견 종합 플랜.
러닝 골든마스터 통과(불변).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: insights.ts — 자전거 콘텐츠 + commute 제거

**Files:**
- Modify: `lib/insights.ts`

**Interfaces:**
- Consumes: `ActivityKey`("bike" 포함).
- Produces: 모든 `Record<ActivityKey, ...>` 테이블에 bike 키, commute 키 제거. 시그니처 변화 없음.

- [ ] **Step 1: DAY_PART_DESC commute→bike**

```ts
const DAY_PART_DESC: Record<ActivityKey, [string, string, string]> = {
  run: ["상쾌한 아침", "한낮 러닝", "선선한 저녁"],
  walk: ["상쾌한 아침", "한낮 걷기", "선선한 저녁"],
  dog: ["아침 산책", "한낮 산책", "저녁 산책"],
  bike: ["상쾌한 아침 라이딩", "한낮 라이딩", "선선한 저녁 라이딩"]
};
```

- [ ] **Step 2: HEADLINE_SCORE commute→bike**

`HEADLINE_SCORE`의 commute 배열을 bike로 교체:
```ts
  bike: [
    [88, ["라이딩하기 최고예요", "완벽한 라이딩 날씨예요", "페달 밟기 딱 좋아요"]],
    [80, ["라이딩하기 좋아요", "달리기 좋은 바람이에요", "기분 좋은 날이에요"]],
    [72, ["라이딩 무난해요", "탈 만해요", "나쁘지 않아요"]],
    [55, ["보통이에요", "무난한 편이에요", "가볍게 탈 만해요"]],
    [38, ["살짝 아쉬워요", "바람이 아쉬워요", "무리는 금물이에요"]],
    [0, ["오늘은 쉬어가요", "실내가 나아요", "라이딩 어려워요"]]
  ]
```

- [ ] **Step 3: SUBLINES commute→bike**

`SUBLINES`의 commute 블록을 bike로 교체:
```ts
  bike: {
    rain: ["젖은 노면은 위험해요", "타이어 미끄럼 조심해요", "오늘은 실내가 나아요"],
    rainMaybe: ["비 오기 전 짧게 타요", "노면 상태 확인해요", "라이트 챙기면 좋아요"],
    dustBad: ["오래 타는 건 피해요", "마스크 쓰기 애매해요", "강도 낮춰서 타요"],
    dustSoso: ["큰길보다 강변길로요", "짧게 타고 마쳐요", "숨차면 속도 낮춰요"],
    hot2: ["물 꼭 챙겨요", "그늘 코스로 돌아요", "속도 낮춰서 타요"],
    hot1: ["수분 자주 챙겨요", "초반부터 천천히요", "무리한 언덕 피해요"],
    cold2: ["방풍 단단히 해요", "장갑 꼭 껴요", "손발 시림 조심해요"],
    cold1: ["바람막이 챙겨요", "귀·손 보온해요", "초반 천천히 데워요"],
    windy: ["맞바람 구간 주의해요", "강풍엔 무리 말아요", "다리·둑길 조심해요"],
    uv: ["아이웨어 챙겨요", "선크림 바르고요", "그늘 코스로요"],
    humid: ["끈적여도 통풍 유지해요", "물 자주 마셔요", "편한 속도로요"],
    score: [
      [88, ["긴 코스 도전 좋아요", "맞바람도 즐길 만해요", "끝나고 스트레칭 잊지 마요"]],
      [80, ["편한 기어로 즐겨요", "리듬 잡기 좋아요", "가볍게 몸 풀고 가요"]],
      [72, ["부담 없이 다녀와요", "짧게라도 좋아요", "안전 장비 챙겨요"]],
      [55, ["짧은 코스 추천해요", "가까운 강변 어때요", "상태 보며 타요"]],
      [38, ["가볍게만 타요", "안전 위주로요", "짧게만 다녀와요"]],
      [0, ["실내 자전거가 나아요", "오늘은 쉬어가요", "내일을 노려봐요"]]
    ]
  }
```

- [ ] **Step 4: ONE_LINERS commute→bike**

`ONE_LINERS`의 commute 블록을 bike로 교체:
```ts
  bike: {
    rain: [
      "비 오는 날 라이딩은 제동거리가 길어지고 미끄러워요. 오늘은 실내 자전거가 안전해요.",
      "노면이 젖으면 코너와 브레이크가 위험해요. 꼭 타야 하면 속도를 크게 낮추세요."
    ],
    rainMaybe: [
      "비 올 확률이 높아요. 짧은 코스로 잡고 라이트와 방수 겉옷을 챙기세요.",
      "비 소식이 있어요. 나간다면 젖은 노면 대비해 여유 있게 타세요."
    ],
    dust: [
      "미세먼지가 있어 오래 타긴 아쉬워요. 강변 코스로 짧게, 숨차면 속도를 낮추세요.",
      "공기가 탁한 날이에요. 큰길보다 자전거도로로, 평소보다 짧게 다녀오세요."
    ],
    hot: [
      "더위엔 라이딩 중 탈수가 빨라요. 물통을 꼭 챙기고 그늘 많은 코스로 도세요.",
      "한낮 더위는 체력 소모가 커요. 아침저녁 시원할 때 타는 게 훨씬 좋아요."
    ],
    uv: [
      "햇살이 강한 시간이에요. 아이웨어와 선크림을 챙기고 그늘 코스로 도세요.",
      "자외선이 강해 눈이 쉽게 지쳐요. 선글라스는 라이딩 안전에도 도움이 돼요."
    ],
    wind: [
      "바람이 강해요. 나갈 때 맞바람으로 시작하면 돌아올 때 등바람이라 편해요.",
      "강풍엔 자전거가 휘청여 위험해요. 다리·둑길·트인 구간을 특히 조심하세요."
    ],
    humid: [
      "습한 날은 땀이 안 말라 끈적여요. 통풍 되는 저지에 편한 기어로 타세요.",
      "끈적한 날씨예요. 물을 자주 마시고 무리한 언덕은 피하세요."
    ],
    cold: [
      "쌀쌀한데 라이딩은 체감이 더 떨어져요. 방풍 자켓과 장갑은 필수예요.",
      "추운 날은 손발이 먼저 시려요. 방풍 장갑과 넥워머로 초반을 버티세요."
    ],
    good: [
      "라이딩하기 좋아요. 초반은 편한 기어로 몸을 풀고 후반에 리듬을 올려보세요.",
      "바람도 적당한 좋은 날이에요. 안전 장비 챙기고 즐겁게 페달을 밟아보세요.",
      "몸이 가볍게 느껴질 날씨예요. 긴 코스도 부담 없이 다녀올 수 있어요."
    ],
    ok: [
      "무난한 라이딩 컨디션이에요. 가까운 강변 코스로 가볍게 다녀오세요.",
      "나쁘지 않은 날이에요. 안전 위주로 짧게 타고 오면 딱 좋아요."
    ],
    bad: [
      "라이딩엔 아쉬운 날이에요. 무리하기보다 실내 자전거나 휴식이 나아요.",
      "굳이 탄다면 아주 짧게, 안전 장비를 꼭 갖추고 다녀오세요."
    ]
  }
```

- [ ] **Step 5: BRIEFING_VERDICT commute→bike + caution/tip 분기**

`BRIEFING_VERDICT`의 commute를 bike로:
```ts
  bike: [
    [88, ["완벽한 라이딩 날씨예요", "오늘 안 타면 아까워요", "라이딩 최고의 날이에요", "이런 바람 흔치 않아요"]],
    [72, ["라이딩하기 좋은 날이에요", "가볍게 타기 딱 좋아요", "기분 좋게 달릴 수 있어요", "부담 없이 나가기 좋아요"]],
    [55, ["무난한 편이에요", "짧은 코스는 충분해요", "바람 보며 타봐요", "가볍게라면 괜찮아요"]],
    [38, ["짧게만 추천해요", "안전 위주로 타요", "무리는 아쉬워요", "가까운 코스만요"]],
    [0, ["오늘은 실내가 나아요", "쉬어가도 괜찮아요", "무리할 날은 아니에요", "안전이 우선이에요"]]
  ]
```
`composeBriefing` 안 `isDog`/`isRun` 분기에 bike 케이스가 필요한 지점(caution·tip)은 아래로 보강. `caution`의 wind 케이스와 tip의 wind 케이스에 bike 문구 추가:
```ts
      case "wind":
        return activity === "run" ? "바람 강함, 맞바람 먼저" : activity === "bike" ? "강풍 주의, 안전 우선" : "바람 강함, 겉옷 챙기기";
```
tip 부분 `generic` 객체에 bike 키 추가:
```ts
      bike: ["출발 전 타이어 공기 확인", "헬멧·장갑 착용", "물통 챙기기", "라이트·반사판 점검", "맞바람 구간 천천히", "내리막 제동 여유 있게"],
```
그리고 tip의 조건부(비/더위/추위/자외선/바람) 중 wind 케이스: `if (reference.windSpeed >= 8) return activity === "run" ? "바람막이 한 겹" : activity === "bike" ? "강풍 구간 감속" : "겉옷 한 겹 챙기기";`

- [ ] **Step 6: getMetricDetail — METRIC_WORDS + isBike 분기**

`METRIC_WORDS`의 commute→bike:
```ts
  bike: { doing: "라이딩", doingGood: "라이딩", hard: "오래 라이딩" },
```
`getMetricDetail` 함수 상단에 `const isBike = activity === "bike";` 추가. wind 지표 meaning/tip에 자전거 특화 분기:
- wind caution meaning에 `isBike ? "바람이 강해요. 자전거가 휘청여 위험할 수 있어요." : ...` 추가.
- wind tip에 `isBike ? "강풍엔 다리·둑길·트인 구간이 특히 위험해요. 감속하세요." : ...` 추가.
- precip meaning amount>=1에 `isBike ? "비가 내려요. 노면이 미끄럽고 제동거리가 길어져 위험해요." : ...` 추가.

- [ ] **Step 7: 검증**

Run: `pnpm test && pnpm typecheck && pnpm build 2>&1 | tail -4`
Expected: 전부 통과, `commute` 잔재 없음.

- [ ] **Step 8: Commit**

```bash
git add lib/insights.ts
git commit -m "feat: insights 자전거 콘텐츠 + 출퇴근 제거

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: outfit.ts — 자전거 복장

**Files:**
- Modify: `lib/outfit.ts`

**Interfaces:**
- Consumes: `ActivityKey`("bike").
- Produces: bike 분기(현재 else 분기가 걷기류였음 → bike 명시 분기 추가).

- [ ] **Step 1: mainFor/topFor/bottomFor/headlineFor에 bike 분기 추가**

`mainFor`에 자전거 분기(방풍 강조):
```ts
  if (activity === "bike") {
    if (feel >= 23) return "저지 + 반바지";
    if (feel >= 15) return "긴팔 저지 + 긴바지";
    if (feel >= 8) return "바람막이 + 긴바지";
    if (feel >= 2) return "방풍 자켓 + 기모 하의";
    return "방한 방풍 라이딩복";
  }
```
`topFor`에 bike 분기:
```ts
  if (activity === "bike") {
    if (feel >= 23) return { value: "반팔 저지", reason: "속도 바람에 금방 시원해져요. 통풍 되는 저지로." };
    if (feel >= 15) return { value: "긴팔 저지", reason: "달릴 때 바람을 막아주는 한 겹이 좋아요." };
    if (feel >= 8) return { value: "바람막이 자켓", reason: "속도 체감이 커요. 방풍이 핵심이에요." };
    if (feel >= 2) return { value: "방풍 자켓 + 이너", reason: "찬 바람을 정면으로 맞아요. 방풍 필수예요." };
    return { value: "방한 방풍 상의(레이어)", reason: "여러 겹에 방풍을 더하세요." };
  }
```
`bottomFor`에 bike 분기:
```ts
  if (activity === "bike") {
    if (feel >= 20) return { value: "반바지 또는 7부", reason: "페달 편한 신축 소재로." };
    if (feel >= 10) return { value: "긴바지(패드 있으면 좋음)", reason: "무릎 보온과 안장 편의를 챙겨요." };
    if (feel >= 2) return { value: "기모 긴바지", reason: "다리 보온이 라이딩 지속력을 좌우해요." };
    return { value: "방풍 기모 하의", reason: "찬 바람 정면 대비가 필요해요." };
  }
```
`headlineFor`에 bike 분기:
```ts
  if (activity === "bike") {
    if (feel >= 26) return "더운 라이딩이에요. 통풍 저지에 물통은 필수, 그늘 코스를 노리세요.";
    if (feel >= 15) return "라이딩 좋은 온도예요. 얇게 입고 아이웨어만 더하면 완벽해요.";
    if (feel >= 8) return "속도 바람이 서늘해요. 바람막이 한 겹이면 딱 좋아요.";
    if (feel >= 2) return "찬 바람을 정면으로 맞아요. 방풍 자켓과 장갑을 꼭 챙기세요.";
    return "한파 라이딩이에요. 방풍·방한 단단히 하고 짧게 다녀오세요.";
  }
```

- [ ] **Step 2: getOutfitPlan에 bike 전용 준비물 카테고리**

`getOutfitPlan`의 애견 전용 블록(`if (isDog)`) 아래에 자전거 블록 추가:
```ts
  const isBike = activity === "bike";
  if (isBike) {
    categories.push({ emoji: "⛑️", label: "안전 장비", value: "헬멧 + 장갑", reason: "라이딩 안전의 기본이에요." });
    if (current.uvIndex >= 3 || feel >= 20) {
      categories.push({ emoji: "🕶️", label: "아이웨어", value: "라이딩 고글", reason: "바람·벌레·자외선에서 눈을 지켜요." });
    }
    if (current.precipitation >= 0.3 || current.precipitationProbability >= 60 || current.uvIndex <= 1) {
      categories.push({ emoji: "🔦", label: "라이트", value: "전조등 + 후미등", reason: "흐리거나 비 오면 시야 확보가 안전을 좌우해요." });
    }
  }
```
(`isBike`는 함수 상단 `const isRun = ...` 옆에 선언.)

- [ ] **Step 3: getOutfit(간이) extras에 bike 반영**

`getOutfit`의 extras 로직에 자전거 안전 장비:
```ts
  if (activity === "bike") extras.push("헬멧");
```
(feel<3 장갑 push 뒤에 추가. slice(0,3)이 이미 있으니 개수 안전.)

- [ ] **Step 4: 검증 + Commit**

Run: `pnpm typecheck && pnpm test`
```bash
git add lib/outfit.ts
git commit -m "feat: 자전거 복장(방풍·헬멧·아이웨어·라이트)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: page.tsx — 텍스트 audit + 좌측 레일 제거 + 애견 모듈 + 자전거 안내

**Files:**
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: `ACTIVITIES[activity].terms`, `getDogPlan`, `getPawRisk`(getDogPlan 내부), profile.
- Produces: 없음(내부 렌더 변경).

- [ ] **Step 1: import 정리 (commute 로직 제거, dog 플랜 추가)**

`import { ACTIVITIES, ACTIVITY_ORDER, getPawRisk, getUmbrellaAdvice, type ActivityKey } from "@/lib/activity";`
→ `import { ACTIVITIES, ACTIVITY_ORDER, getDogPlan, type ActivityKey } from "@/lib/activity";`

- [ ] **Step 2: pawRisk/umbrella useMemo → dogPlan useMemo로 교체**

기존 `pawRisk`, `umbrella` useMemo 두 개를 삭제하고:
```tsx
const dogPlan = useMemo(() => {
  if (activity !== "dog" || !view) return null;
  const ref = isTomorrow ? view.best : view.reference;
  return getDogPlan({
    score: ref.totalScore, hour: ref.hour, temperature: ref.temperature,
    apparentTemperature: ref.apparentTemperature, uvIndex: ref.uvIndex,
    precipitation: ref.precipitation, precipitationProbability: ref.precipitationProbability
  });
}, [activity, view, isTomorrow]);

// 자전거 강풍 안내
const bikeWind = useMemo(() => {
  if (activity !== "bike" || !view) return null;
  const ref = isTomorrow ? view.best : view.reference;
  if (ref.windSpeed >= 11) return { level: "danger", text: "강풍이에요. 자전거가 휘청일 수 있으니 다리·둑길을 조심하세요." };
  if (ref.windSpeed >= 7) return { level: "caution", text: "바람이 조금 있어요. 맞바람 구간은 속도를 낮추세요." };
  return null;
}, [activity, view, isTomorrow]);
```

- [ ] **Step 3: 히어로 아래 애견/자전거 안내 렌더 교체**

기존 `{pawRisk ? (...) : null}` 와 `{umbrella ? (...) : null}` 블록을 아래로 교체:
```tsx
{/* 애견산책 전용 — 신호등·산책길이·발바닥·체크리스트 */}
{dogPlan ? (
  <section className={`dog-plan dog-${dogPlan.signal}`} aria-label="산책 안내">
    <div className="dog-signal">
      <span className="dog-dot" aria-hidden="true" />
      <strong>{dogPlan.signalText}</strong>
      <span className="dog-length">{dogPlan.walkLength}</span>
    </div>
    <div className={`dog-paw paw-${dogPlan.paw.level}`}>
      <span aria-hidden="true">{dogPlan.paw.level === "danger" ? "🚨" : dogPlan.paw.level === "caution" ? "⚠️" : "🐾"}</span>
      <div>
        <b>{dogPlan.paw.title}</b>
        <p>{dogPlan.paw.detail}</p>
      </div>
    </div>
    <ul className="dog-check">
      {dogPlan.checklist.map((c) => (
        <li key={c}>{c}</li>
      ))}
    </ul>
  </section>
) : null}

{/* 자전거 강풍 안내 */}
{bikeWind ? (
  <section className={`advisory advisory-${bikeWind.level === "danger" ? "danger" : "caution"}`} aria-label="바람 안내">
    <span className="advisory-emoji" aria-hidden="true">💨</span>
    <div className="advisory-body">
      <strong>{bikeWind.level === "danger" ? "강풍 주의" : "바람 주의"}</strong>
      <p>{bikeWind.text}</p>
    </div>
  </section>
) : null}
```

- [ ] **Step 4: 좌측 레일 제거 + 상단 탭 유지**

`<div className="side-left"> ... </div>` 블록을 원래대로:
```tsx
        <AdSlot side="left" />
```
(ActivityRail rail 인스턴스 삭제. 모바일용 `variant="tabs"`는 그대로 두되 CSS에서 데스크탑에도 보이게 — Task 6.)

- [ ] **Step 5: 텍스트 audit — terms 참조로 교체**

`const t = profile.terms;`를 view 계산부 근처(렌더 상단, profile 선언 뒤)에 추가하고 아래를 교체:
- 데이파트 점수 태그: `{part.rainy ? \`우중런 ${part.best.totalScore}점\` : ...}` → `{part.rainy ? \`${t.rainyTag} ${part.best.totalScore}점\` : ...}`
- 데이파트 차단: `러닝 어려움` → `{t.blockedTag}`
- 복장 진입 카드 `러닝 복장 · 눌러서 자세히` → `{t.outfitTitle} · 눌러서 자세히`
- rank 카드 nowTag: 기존 인라인 분기를 `{t.nowTag}`로 단순화
- fired-alarm `러닝 시간이에요!`/`러닝화 신고...` → `{firedAlarm.timeLabel} ${profile.label} 시간이에요!` / `지금 나가면 딱 좋아요. 안전하게 출발해볼까요?`

- [ ] **Step 6: 시트 제목·알림 문구 activity화**

- OutfitSheet: props에 `activity` 이미 있음. 제목 `지금 러닝 복장 · 체감` → `지금 {ACTIVITIES[activity].terms.outfitTitle} · 체감`. (OutfitSheet 안에서 `ACTIVITIES[activity].terms.outfitTitle` 사용.)
- MetricSheet `🟢 러닝 좋은 {hour}시` → `🟢 {ACTIVITIES[activity].label} 좋은 {hour}시` (activity prop 이미 있음).
- AlarmSheet 제목 `🔔 러닝 알림`: AlarmSheet에 `activity` prop 추가 후 `🔔 {ACTIVITIES[activity].terms.alarmTitle}`. 호출부 `<AlarmSheet ... activity={activity} />`.
- 알림 예약 라벨(`{part.label} 러닝`, `추천 러닝`): `{part.label} ${profile.label}`, `추천 ${profile.label}`.
- showRunningNotification/알림 본문 `러닝하기 좋은 시간이에요`: 이 함수는 모듈 최상단(컴포넌트 밖)이라 activity를 인자로 받게 시그니처 확장 — `showRunningNotification(alarm, readyText)` 형태로 호출부에서 `profile.terms.notifReady` 전달. (본문 `${alarm.label} · ${readyText}`.)
- RecordSheet 제목 `🏃 나의 러닝 기록` → `🏃 나의 활동 기록`, aria-label `러닝 기록` → `활동 기록`.

- [ ] **Step 7: 검증 (브라우저)**

Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
그다음 preview로 4개 활동 순회 — 각 화면에 다른 활동 표현이 없는지 확인. 애견은 신호등 카드, 자전거는 강풍 안내·라이딩 문구 확인.

- [ ] **Step 8: 러닝 표현 누수 grep 검증**

Run: `grep -n "우중런\|러닝 복장\|러닝 알림\|러닝하기\|나의 러닝" app/page.tsx`
Expected: 러닝(run) 전용 컨텍스트를 제외하곤 결과 없음. terms.rainyTag 등으로 대체됐는지 확인.

- [ ] **Step 9: Commit**

```bash
git add app/page.tsx
git commit -m "feat: 텍스트 활동화 audit + 좌측레일 제거 + 애견 모듈 + 자전거 강풍 안내

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: globals.css — 색상 위계 재설계 + 상단탭 데스크탑 + 애견/좌측 정리

**Files:**
- Modify: `app/globals.css`

- [ ] **Step 1: rank 카드 액센트바 재설계 (앰버 색면 제거)**

`.rank-1` 규칙을 색면 그라디언트 대신 좌측 액센트바로 교체:
```css
.rank-card { position: relative; overflow: hidden; }
.rank-card::before {
  content: ""; position: absolute; left: 0; top: 0; bottom: 0; width: 5px; background: #c3cfe2;
}
.rank-1 { background: var(--card); border-color: var(--line); box-shadow: var(--shadow-sm); }
.rank-1::before { background: linear-gradient(180deg, #f5b323, #f59023); }
.rank-2::before { background: linear-gradient(180deg, #aab6cc, #8f9fb8); }
.rank-3::before { background: linear-gradient(180deg, #d3a17a, #b9835c); }
.rank-card { padding-left: 18px; }
```
(1등의 넓은 크림-앰버 배경 제거 → 일출 카드와 색 충돌 해소. 골드는 뱃지+액센트바로만.)

- [ ] **Step 2: 일출 카드 대비 강화 (선택적 미세조정)**

일출/일몰이 서로·주변과 명확히 구분되게 채도 살짝 상향:
```css
.sun-rise { background: linear-gradient(135deg, #ffedd0, #fffaf3); border-color: #ffd699; }
.sun-set { background: linear-gradient(135deg, # eae4ff, #faf8ff); border-color: #d3c8f2; }
```
(1등 카드가 흰색이 되었으므로 일출의 웜톤이 유일한 웜 카드가 되어 구분됨.)

- [ ] **Step 3: 상단 탭 데스크탑에서도 표시 + 좌측 레일 CSS 제거**

`.activity-tabs { display: none; ... }`의 기본을 `display: flex`로 바꾸고, `.side-left`/`.activity-rail` 관련 규칙과 `@media (max-width:1080px){ .side-left{display:none} .activity-tabs{display:flex} }` 블록 삭제. 대신 상단 탭을 중앙 정렬·최대폭 제한:
```css
.activity-tabs { display: flex; justify-content: center; gap: 8px; overflow-x: auto; padding: 2px 2px 12px; scrollbar-width: none; }
```
(좌측 광고 `.side-ad`는 기존 `@media (max-width:1080px){ .side-ad{display:none} }` 규칙 유지 — 광고만 반응형.)

- [ ] **Step 4: 애견 플랜 카드 스타일**

globals.css 끝에 추가:
```css
.dog-plan { margin-top: 12px; padding: 14px 16px; border-radius: 16px; background: var(--card); border: 1.5px solid var(--line); box-shadow: var(--shadow-sm); display: flex; flex-direction: column; gap: 12px; }
.dog-signal { display: flex; align-items: center; gap: 10px; }
.dog-signal strong { font-size: 1.05rem; font-weight: 850; }
.dog-length { margin-left: auto; font-size: 0.82rem; font-weight: 700; color: var(--ink-2); background: var(--brand-soft); padding: 4px 10px; border-radius: 999px; }
.dog-dot { width: 12px; height: 12px; border-radius: 50%; flex: none; }
.dog-go .dog-dot { background: var(--good); }
.dog-caution .dog-dot { background: var(--warn); }
.dog-avoid .dog-dot { background: var(--bad); }
.dog-go .dog-signal strong { color: var(--good-deep); }
.dog-caution .dog-signal strong { color: var(--warn-deep); }
.dog-avoid .dog-signal strong { color: var(--bad-deep); }
.dog-paw { display: flex; gap: 10px; align-items: flex-start; padding: 10px 12px; border-radius: 12px; background: #f7f9fc; }
.dog-paw b { font-size: 0.9rem; font-weight: 800; }
.dog-paw p { margin: 2px 0 0; font-size: 0.82rem; line-height: 1.5; color: var(--ink-2); }
.dog-paw.paw-danger { background: #fff3f3; }
.dog-paw.paw-caution { background: #fff9ee; }
.dog-check { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; gap: 6px; }
.dog-check li { font-size: 0.8rem; font-weight: 700; color: var(--ink-2); background: #eef2f8; border-radius: 999px; padding: 5px 11px; }
```

- [ ] **Step 5: 브라우저 검증**

preview로: 러닝(1등 카드 흰+골드바, 일출 웜톤 구분 확인), 애견(신호등 카드), 자전거(강풍 안내), 데스크탑/모바일 상단 탭. 콘솔 에러 0.

- [ ] **Step 6: Commit**

```bash
git add app/globals.css
git commit -m "style: 색상 위계 재설계(1등/일출 구분) + 상단탭 통일 + 애견 카드

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 최종 검증 + 문서 + v0.3.0 배포

**Files:**
- Modify: `package.json`, `CHANGELOG.md`, `docs/ROADMAP.md`, `docs/DEVELOPMENT_LOG.md`

- [ ] **Step 1: 전체 검증**

Run: `pnpm test && pnpm typecheck && rm -rf .next && pnpm build`
Expected: 골든마스터 포함 전 테스트 PASS, 빌드 성공.

- [ ] **Step 2: 러닝 회귀·전 활동 육안 확인 (preview)**

러닝 화면 v0.2.0과 동일, 4개 활동 문구/색/경고 정상. `grep -rn "commute\|출퇴근\|우중런\|getUmbrella" app/ lib/` → 러닝 rainyTag 정의 외 잔재 없음.

- [ ] **Step 3: 버전·문서**

- `package.json` version `0.2.0` → `0.3.0`.
- `CHANGELOG.md` `[0.3.0]`: 자전거 교체, 텍스트 전면 활동화, 색상 재설계, 애견 모듈, 상단탭 통일.
- `ROADMAP.md`·`DEVELOPMENT_LOG.md` 갱신.

- [ ] **Step 4: 커밋·태그·배포**

```bash
git add -A
git commit -m "release: v0.3.0 — 자전거·텍스트/색상 정비·애견 강화

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git tag -a v0.3.0 -m "v0.3.0 — 자전거 교체, 텍스트 활동화, 색상 위계, 애견 모듈"
git push origin main && git push origin v0.3.0
```

- [ ] **Step 5: 배포 검증**

`curl -s -o /dev/null -w "%{http_code}" https://runningcall.vercel.app` → 200. 실배포에 자전거 탭 노출 확인.

---

## Self-Review

- [ ] 스펙 5개 문제 전부 대응: 색상(Task 6), 텍스트(Task 3·5), 출퇴근→자전거(Task 1~4), 애견(Task 1·5·6), 선택 UI(Task 5·6).
- [ ] 러닝 골든마스터로 불변 잠금(Task 2). windCap run={14,40}로 동일.
- [ ] commute 완전 제거(활동순서·COMMENTS·테이블·outfit·page).
- [ ] 카피 격리 grep 검증(Task 5 Step 8, Task 7 Step 2).
- [ ] 타입 일관: ActivityKey "bike", terms/windCap 필드, getDogPlan/DogPlan 시그니처 태스크 간 일치.
