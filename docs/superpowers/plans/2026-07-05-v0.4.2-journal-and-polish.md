# v0.4.2 운동 일지 + 동적 가이드 + UI 정리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 전체 운동 일지(다이어리)를 신설하고, 가이드를 날씨 조건에 맞춰 동적으로 만들고, 좌측 레일을 큰 글자로 정리하고, 등산 3구간·애견 플랜 심화·히어로 칩 모바일 레이아웃·활동별 위치 기억을 적용하고, 죽은 코드를 제거한다.

**Architecture:** 일지는 `lib/journal.ts`의 날짜별 저장(activities+note)을 기존 `activity-log`와 연동한다. 동적 가이드는 현재 slot을 받아 조건부 블록 하나를 만들어 정적 가이드 위에 얹는다. 위치는 활동별 맵을 추가하되 즐겨찾기는 전역 그대로 둔다.

**Tech Stack:** Next.js 16, React 19, TypeScript, Vitest.

## Global Constraints
- **러닝 불변**: run 점수·톤·멘트 불변, `pnpm test` 골든마스터 통과 필수.
- 기존 v0.4.1 활동별 기록 데이터(`running-alarm:activity-log:v1`) 보존.
- 새 파일 첫 줄 한글 주석, 한글 마침표 종결.
- 커밋 트레일러 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- PATH: `export PATH="/opt/homebrew/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin:/Users/runner706/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH"` + `cd "/Users/runner706/Developer/러닝콜"`.

---

## File Structure
- **Create** `lib/journal.ts` — 일지 저장 shape·CRUD·activity-log 연동.
- **Create** `lib/__tests__/journal.test.ts`.
- **Modify** `lib/activity.ts` — DogPlan에 reason/alternative/careTip.
- **Modify** `lib/insights.ts` — HIKE_DAY_PARTS 3구간, 죽은 코드 삭제(Briefing/composeBriefing/BRIEFING_VERDICT/shortReason/heroReason).
- **Modify** `lib/weather.ts` — findTomorrowMorningBest 삭제.
- **Modify** `lib/activity-guide.ts` — getDynamicGuideBlock 추가.
- **Modify** `app/page.tsx` — ActivityRail 단순화+일지버튼, activity-tagline 제거, JournalView, 활동별 위치, DogPlan 카드 확장, 미사용 import 정리.
- **Modify** `app/globals.css` — 칩 모바일 그리드, 레일 큰 글자, 일지 스타일.

---

## Task 1: 죽은 코드 제거

**Files:** `lib/insights.ts`, `lib/weather.ts`, `app/page.tsx`

- [ ] **Step 1: insights.ts에서 삭제**
  - `export type Briefing = {...}` 블록(line 150 부근) 삭제.
  - `const BRIEFING_VERDICT: Record<ActivityKey, ...> = {...}` 전체(line 777 부근) 삭제.
  - `export function composeBriefing(...)` 전체(line 815부터 함수 끝까지) 삭제.
  - `export function shortReason(slot: RunningSlot) {...}` 삭제.
  - `export function heroReason(slot: RunningSlot) {...}` 삭제.
  - 삭제 후 `factorsOf`가 다른 곳에서 안 쓰이면(shortReason/heroReason 전용이었다면) 같이 삭제, 다른 곳에서 쓰이면 유지. `grep -n "factorsOf" lib/insights.ts`로 확인.
- [ ] **Step 2: weather.ts에서 삭제** — `export function findTomorrowMorningBest(tomorrow: RunningSlot[]) {...}` 전체 삭제.
- [ ] **Step 3: page.tsx 미사용 import 정리** — lucide `Bell` import 제거(사용처 없으면), `todayStr`, `weekDates`를 `lib/record` import에서 제거, `RunningForecast` 타입 import를 `lib/weather` import에서 제거. 각각 `grep -n "\bBell\b\|\btodayStr\b\|\bweekDates\b\|\bRunningForecast\b" app/page.tsx`로 실제 사용처 0인지 확인 후 제거.
- [ ] **Step 4: 검증**
  Run: `pnpm typecheck 2>&1 | tail -20`
  Expected: 에러 없음(미사용 삭제로 인한 참조 누락 시 즉시 드러남).
  Run: `pnpm test && pnpm build 2>&1 | tail -3`
- [ ] **Step 5: Commit**
  ```bash
  git add lib/insights.ts lib/weather.ts app/page.tsx
  git commit -m "chore: 죽은 코드 제거 (composeBriefing/Briefing/BRIEFING_VERDICT, findTomorrowMorningBest, shortReason/heroReason, 미사용 import)

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 2: 히어로 조건 칩 — 모바일 2열 그리드

**Files:** `app/globals.css`

- [ ] **Step 1: 모바일 브레이크포인트에서 그리드로 전환**

`app/globals.css`의 모바일 `.status-chips` 규칙(line 1667 부근, `@media (max-width: 620px)` 블록 안)을 다음으로 교체:
```css
  .status-chips {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 6px;
    margin-top: 9px;
    width: 100%;
  }

  .status-chip {
    width: 100%;
    justify-content: center;
    height: 29px;
    padding: 0 11px;
    font-size: 0.78rem;
  }
```
(기존 `.status-chip` 규칙과 병합 — 중복 선언 없게 하나로 정리.)

- [ ] **Step 2: 브라우저 검증**
  preview 375px에서 조건 칩 2개가 나란히 한 줄에 정렬되는지 확인(칩 1개면 왼쪽 정렬 유지되는지도 확인 — grid라 1개면 한 칸만 채움, 문제 없음).

- [ ] **Step 3: Commit**
  ```bash
  git add app/globals.css
  git commit -m "style: 히어로 조건 칩 모바일 2열 그리드로 정렬

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 3: 애견산책 플랜 심화 — reason/alternative/careTip

**Files:** `lib/activity.ts`, `app/page.tsx`

**Interfaces:**
- Produces: `DogPlan`에 `reason: string; alternative: string; careTip: string` 추가.

- [ ] **Step 1: DogPlan 타입 확장 + getDogPlan 로직**

`lib/activity.ts`의 `DogPlan` 타입과 `getDogPlan` 함수를 다음으로 교체:
```ts
export type DogPlan = {
  signal: "go" | "caution" | "avoid";
  signalText: string;
  reason: string;
  alternative: string;
  careTip: string;
  walkLength: string;
  paw: PawRisk;
  checklist: string[];
};

export function getDogPlan(input: {
  score: number;
  hour: number;
  temperature: number;
  apparentTemperature: number;
  uvIndex: number;
  precipitation: number;
  precipitationProbability: number;
}): DogPlan {
  const paw = getPawRisk({ hour: input.hour, temperature: input.temperature, uvIndex: input.uvIndex });

  let signal: DogPlan["signal"];
  if (input.score >= 60 && paw.level !== "danger") signal = "go";
  else if (input.score >= 40 && paw.level !== "danger") signal = "caution";
  else signal = "avoid";

  const signalText = signal === "go" ? "산책 가능" : signal === "caution" ? "짧게 주의" : "산책 자제";

  // 판정 근거 — 가장 나쁜 요인을 우선순위로 짚어준다
  const rainy = input.precipitation >= 0.3 || input.precipitationProbability >= 60;
  const reason =
    paw.level === "danger"
      ? "지면이 너무 뜨거워요(발바닥 화상 위험)"
      : rainy
      ? "비 소식이 있어요"
      : input.apparentTemperature >= 28
      ? "더위가 심해요"
      : input.apparentTemperature <= -5
      ? "한파예요"
      : signal === "go"
      ? "날씨 조건이 좋아요"
      : "컨디션이 애매한 날씨예요";

  const alternative =
    signal === "avoid"
      ? "배변만 짧게 다녀오고, 실내 노즈워크·터그 놀이로 에너지를 풀어주세요"
      : signal === "caution"
      ? "평소보다 짧은 코스로, 강아지 반응을 보며 다녀오세요"
      : "";

  const careTip = rainy
    ? "다녀오면 발과 배를 꼼꼼히 닦아주세요"
    : paw.level !== "safe"
    ? "그늘길로 걷고, 자주 발바닥 상태를 확인하세요"
    : input.apparentTemperature <= 3
    ? "다녀와서 몸을 따뜻하게 해주세요"
    : "물을 챙겨 중간중간 마시게 해주세요";

  const walkLength =
    signal === "avoid"
      ? "배변만 짧게 (5분 이내)"
      : input.score >= 75 && paw.level === "safe"
      ? "길게 걸어도 좋아요 (40분+)"
      : input.score >= 55
      ? "보통 산책 (20~30분)"
      : "짧게 (10분 이내)";

  const checklist: string[] = ["💧 물", "🛍️ 배변봉투"];
  if (input.apparentTemperature >= 25) checklist.push("🌳 그늘길 코스");
  if (input.apparentTemperature <= 3) checklist.push("🧥 강아지 옷");
  if (rainy) checklist.push("🧻 발 닦을 수건");
  if (paw.level !== "safe") checklist.push("🐾 지면 온도 확인");

  return { signal, signalText, reason, alternative, careTip, walkLength, paw, checklist };
}
```

- [ ] **Step 2: page.tsx 렌더 확장**

`app/page.tsx`의 dog-plan 섹션(`.dog-signal` 아래)에 reason/alternative/careTip 노출 추가:
```tsx
                <section className={`dog-plan dog-${dogPlan.signal}`} aria-label="산책 안내">
                  <div className="dog-signal">
                    <span className="dog-dot" aria-hidden="true" />
                    <strong>{dogPlan.signalText}</strong>
                    <span className="dog-length">{dogPlan.walkLength}</span>
                  </div>
                  <p className="dog-reason">{dogPlan.reason}</p>
                  {dogPlan.alternative ? <p className="dog-alt">💡 {dogPlan.alternative}</p> : null}
                  <div className={`dog-paw paw-${dogPlan.paw.level}`}>
                    <span aria-hidden="true">
                      {dogPlan.paw.level === "danger" ? "🚨" : dogPlan.paw.level === "caution" ? "⚠️" : "🐾"}
                    </span>
                    <div>
                      <b>{dogPlan.paw.title}</b>
                      <p>{dogPlan.paw.detail}</p>
                    </div>
                  </div>
                  <p className="dog-care">🧴 {dogPlan.careTip}</p>
                  <ul className="dog-check">
                    {dogPlan.checklist.map((c) => (
                      <li key={c}>{c}</li>
                    ))}
                  </ul>
                </section>
```
(`<p className="dog-reason">`, `dog-alt`, `dog-care`는 Task 8 CSS에서 스타일링.)

- [ ] **Step 3: 검증 + Commit**
  Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
  ```bash
  git add lib/activity.ts app/page.tsx
  git commit -m "feat: 애견 플랜에 판정 이유·대안·케어팁 추가

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 4: `lib/journal.ts` — 운동 일지 저장·CRUD

**Files:** Create `lib/journal.ts`, Create `lib/__tests__/journal.test.ts`

**Interfaces:**
- Produces:
  - `type JournalEntry = { activities: ActivityKey[]; note: string }`
  - `type Journal = Record<string, JournalEntry>` (키: "YYYY-MM-DD")
  - `emptyJournal(): Journal`
  - `normalizeJournal(value: unknown): Journal`
  - `setEntry(journal: Journal, date: string, entry: JournalEntry): Journal`
  - `toggleJournalActivity(journal: Journal, date: string, activity: ActivityKey): Journal`
  - `setNote(journal: Journal, date: string, note: string): Journal`
  - `loadJournal(): Journal`, `saveJournal(journal: Journal): void` (localStorage 안전 래퍼)
  - `JOURNAL_KEY = "running-alarm:journal:v1"`

- [ ] **Step 1: 실패 테스트**

`lib/__tests__/journal.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { emptyJournal, normalizeJournal, setEntry, toggleJournalActivity, setNote } from "@/lib/journal";

describe("journal", () => {
  it("빈 일지는 빈 객체", () => {
    expect(emptyJournal()).toEqual({});
  });

  it("setEntry로 날짜에 항목을 넣는다", () => {
    let j = emptyJournal();
    j = setEntry(j, "2026-03-01", { activities: ["run"], note: "좋았다" });
    expect(j["2026-03-01"]).toEqual({ activities: ["run"], note: "좋았다" });
  });

  it("toggleJournalActivity는 활동을 추가/제거하고 없던 날짜는 새로 만든다", () => {
    let j = emptyJournal();
    j = toggleJournalActivity(j, "2026-03-02", "hike");
    expect(j["2026-03-02"].activities).toEqual(["hike"]);
    j = toggleJournalActivity(j, "2026-03-02", "hike");
    expect(j["2026-03-02"].activities).toEqual([]);
  });

  it("setNote는 다른 필드를 건드리지 않는다", () => {
    let j = setEntry(emptyJournal(), "2026-03-03", { activities: ["walk"], note: "" });
    j = setNote(j, "2026-03-03", "산책 좋았음");
    expect(j["2026-03-03"]).toEqual({ activities: ["walk"], note: "산책 좋았음" });
  });

  it("normalizeJournal은 손상된 값을 걸러낸다", () => {
    const j = normalizeJournal({ "2026-01-01": { activities: ["run", "bad"], note: 5 }, bad: "x" });
    expect(j["2026-01-01"]).toEqual({ activities: ["run"], note: "" });
  });
});
```

- [ ] **Step 2: 실행 — 실패 확인**
  Run: `pnpm test lib/__tests__/journal.test.ts`
  Expected: 모듈 없음 실패.

- [ ] **Step 3: 구현**

```ts
// 운동 일지 저장 구조 — 날짜별 활동 체크 + 한 줄 메모 (activity-log와 별개로 캘린더/메모 뷰 제공)
import type { ActivityKey } from "@/lib/activity";

export type JournalEntry = { activities: ActivityKey[]; note: string };
export type Journal = Record<string, JournalEntry>;

export const JOURNAL_KEY = "running-alarm:journal:v1";

const VALID_ACTIVITIES: ActivityKey[] = ["walk", "dog", "run", "hike", "bike"];
const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

export function emptyJournal(): Journal {
  return {};
}

export function normalizeJournal(value: unknown): Journal {
  const out: Journal = {};
  if (value && typeof value === "object") {
    for (const [date, raw] of Object.entries(value as Record<string, unknown>)) {
      if (!DATE_RE.test(date)) continue;
      if (!raw || typeof raw !== "object") continue;
      const activities = (raw as { activities?: unknown }).activities;
      const note = (raw as { note?: unknown }).note;
      out[date] = {
        activities: Array.isArray(activities)
          ? activities.filter((a): a is ActivityKey => VALID_ACTIVITIES.includes(a as ActivityKey))
          : [],
        note: typeof note === "string" ? note : ""
      };
    }
  }
  return out;
}

export function setEntry(journal: Journal, date: string, entry: JournalEntry): Journal {
  return { ...journal, [date]: entry };
}

export function toggleJournalActivity(journal: Journal, date: string, activity: ActivityKey): Journal {
  const current = journal[date] ?? { activities: [], note: "" };
  const has = current.activities.includes(activity);
  const activities = has ? current.activities.filter((a) => a !== activity) : [...current.activities, activity];
  return setEntry(journal, date, { ...current, activities });
}

export function setNote(journal: Journal, date: string, note: string): Journal {
  const current = journal[date] ?? { activities: [], note: "" };
  return setEntry(journal, date, { ...current, note });
}

export function loadJournal(): Journal {
  if (typeof window === "undefined") return emptyJournal();
  try {
    const raw = window.localStorage.getItem(JOURNAL_KEY);
    return raw ? normalizeJournal(JSON.parse(raw)) : emptyJournal();
  } catch {
    return emptyJournal();
  }
}

export function saveJournal(journal: Journal): void {
  if (typeof window === "undefined") return;
  try {
    window.localStorage.setItem(JOURNAL_KEY, JSON.stringify(journal));
  } catch {
    // quota 등 실패는 조용히 무시
  }
}
```

- [ ] **Step 4: 통과 확인 + Commit**
  Run: `pnpm test && pnpm typecheck`
  ```bash
  git add lib/journal.ts lib/__tests__/journal.test.ts
  git commit -m "feat: 운동 일지 저장 구조(lib/journal.ts) — 날짜별 활동+메모, 정규화

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 5: 등산 데이파트 3구간 (저녁 제거)

**Files:** `lib/insights.ts`

- [ ] **Step 1: HIKE_DAY_PARTS에서 저녁 제거**

`HIKE_DAY_PARTS`(line 46 부근)를 3구간으로:
```ts
const HIKE_DAY_PARTS: Array<{ key: DayPartKey; label: string; icon: string; desc: string; range: [number, number] }> = [
  { key: "dawn", label: "새벽", icon: "sunrise", desc: "새벽 산행", range: [4, 7] },
  { key: "morning", label: "아침", icon: "sunrise", desc: "아침 산행", range: [8, 10] },
  { key: "day", label: "낮", icon: "day", desc: "한낮 산행", range: [11, 15] }
];
```
`DAY_PART_DESC.hike`도 3개로: `["새벽 산행", "아침 산행", "한낮 산행"]`.

- [ ] **Step 2: 기존 테스트 확인**

`lib/__tests__/scoring.test.ts`의 "등산 데이파트는 새벽 포함 4구간이다" 테스트를 3구간으로 수정:
```ts
  it("등산 데이파트는 새벽 포함 3구간이다 (저녁 제외)", async () => {
    const { getDayParts } = await import("@/lib/insights");
    const parts = getDayParts([], false, 6, "hike");
    expect(parts.length).toBe(3);
    expect(parts[0].label).toBe("새벽");
    expect(parts.map((p) => p.label)).not.toContain("저녁");
  });
```

- [ ] **Step 3: page.tsx cols-4 → 등산도 cols-3 확인**

`view.parts.length`가 이제 3이 되어 자동으로 `cols-3`(기존 3열 CSS) 적용됨 — 별도 수정 불필요. `grep -n "cols-\${view.parts.length}" app/page.tsx`로 동적 클래스 확인만.

- [ ] **Step 4: 검증 + Commit**
  Run: `pnpm test && pnpm typecheck && pnpm build 2>&1 | tail -3`
  ```bash
  git add lib/insights.ts lib/__tests__/scoring.test.ts
  git commit -m "feat: 등산 데이파트 3구간으로 정리 (저녁 산행 제거, 다른 활동과 통일)

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 6: 동적 가이드 — 날씨 조건 맞춤 블록

**Files:** `lib/activity-guide.ts`, `app/page.tsx`

**Interfaces:**
- Produces: `getDynamicGuideBlock(activity: ActivityKey, slot: RunningSlot): GuideBlock | null`

- [ ] **Step 1: getDynamicGuideBlock 구현**

`lib/activity-guide.ts` 끝에 추가:
```ts
import type { RunningSlot } from "@/lib/scoring";

// 오늘 조건에 맞춰 가이드 상단에 얹는 동적 블록. 해당 없으면 null(정적 가이드만 표시).
export function getDynamicGuideBlock(activity: ActivityKey, slot: RunningSlot): GuideBlock | null {
  const items: string[] = [];

  if (slot.precipitation >= 0.5 || slot.precipitationProbability >= 60) {
    items.push(
      activity === "hike"
        ? "🌧️ 비 소식 — 바위·계단이 미끄러워요. 하산 속도를 절반으로 낮추세요."
        : activity === "bike"
        ? "🌧️ 비 소식 — 제동거리가 길어져요. 속도를 줄이세요."
        : activity === "dog"
        ? "🌧️ 비 소식 — 배변 산책만 짧게, 다녀와서 발을 닦아주세요."
        : "🌧️ 비 소식 — 접지 좋은 신발과 밝은 옷을 챙기세요."
    );
  }
  if (slot.windSpeed >= 9) {
    items.push(
      activity === "hike"
        ? "🌬️ 바람이 강해요 — 능선·정상에서 특히 조심하세요."
        : activity === "bike"
        ? "💨 바람이 강해요 — 맞바람으로 출발해 등바람으로 돌아오세요."
        : "🌬️ 바람이 강해요 — 체감온도가 더 낮게 느껴져요."
    );
  }
  if (slot.uvIndex >= 7) {
    items.push("☀️ 자외선이 강해요 — 모자·선크림을 챙기세요.");
  }
  if (slot.apparentTemperature >= 30) {
    items.push(
      activity === "dog"
        ? "🥵 폭염 — 아스팔트가 뜨거워요. 이른 아침·저녁으로 미루세요."
        : "🥵 폭염 — 물을 넉넉히 챙기고 무리하지 마세요."
    );
  }
  if (slot.pm25 > 55) {
    items.push("😷 미세먼지가 나쁨이에요 — 강도를 낮추고 시간을 줄이세요.");
  }

  if (items.length === 0) return null;
  return { heading: "🔔 오늘 이것만은", items };
}
```

- [ ] **Step 2: page.tsx GuideView에 동적 블록 연결**

`GuideView` 컴포넌트를 수정해 slot을 받고 동적 블록을 위에 렌더:
```tsx
function GuideView({ activity, slot }: { activity: ActivityKey; slot: RunningSlot | null }) {
  const dynamicBlock = slot ? getDynamicGuideBlock(activity, slot) : null;
  return (
    <section className="guide-view" aria-label={`${ACTIVITIES[activity].label} 가이드`}>
      {dynamicBlock ? <GuideBlocks blocks={[dynamicBlock]} /> : null}
      <GuideBlocks blocks={ACTIVITY_GUIDE[activity].guide} />
    </section>
  );
}
```
호출부(`innerTab === "guide"`)를 `<GuideView activity={activity} slot={view?.reference ?? null} />`로.

- [ ] **Step 3: 검증**
  Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
  preview로 강수확률 높은 지역 검색 후 가이드 탭에 "🔔 오늘 이것만은" 블록이 뜨는지, 없는 경우엔 안 뜨는지 확인.

- [ ] **Step 4: Commit**
  ```bash
  git add lib/activity-guide.ts app/page.tsx
  git commit -m "feat: 가이드 탭에 오늘 조건 맞춤 동적 블록 추가

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 7: 좌측 레일 정리(큰 글자+일지버튼) + activity-tagline 제거 + 활동별 위치

**Files:** `app/page.tsx`

- [ ] **Step 1: ActivityRail rail variant 단순화**

`ActivityRail` 컴포넌트에서 rail 분기의 tagline 렌더 제거:
```tsx
            {variant === "rail" ? (
              <span className="act-rail-label">{item.label}</span>
            ) : (
              <span className="act-label">{item.short}</span>
            )}
```
(`act-rail-text`/`<strong>`/`<small>` 구조를 `act-rail-label` 하나로 교체.)

- [ ] **Step 2: 레일에 일지 버튼 추가**

`ActivityRail`이 `variant="rail"`일 때 목록 아래 구분선+버튼을 렌더하도록 props 확장:
```tsx
function ActivityRail({
  activity,
  onChange,
  variant,
  onOpenJournal
}: {
  activity: ActivityKey;
  onChange: (next: ActivityKey) => void;
  variant: "rail" | "tabs";
  onOpenJournal?: () => void;
}) {
  return (
    <nav className={variant === "rail" ? "activity-rail" : "activity-tabs"} aria-label="활동 선택">
      {ACTIVITY_ORDER.map((key) => {
        const item = ACTIVITIES[key];
        const on = key === activity;
        return (
          <button
            key={key}
            type="button"
            className={`act-item${on ? " on" : ""}`}
            aria-pressed={on}
            onClick={() => onChange(key)}
          >
            <span className="act-emoji" aria-hidden="true">
              {item.emoji}
            </span>
            {variant === "rail" ? (
              <span className="act-rail-label">{item.label}</span>
            ) : (
              <span className="act-label">{item.short}</span>
            )}
          </button>
        );
      })}
      {variant === "rail" && onOpenJournal ? (
        <>
          <div className="rail-sep" aria-hidden="true" />
          <button type="button" className="act-item journal-btn" onClick={onOpenJournal}>
            <span className="act-emoji" aria-hidden="true">📔</span>
            <span className="act-rail-label">운동 일지</span>
          </button>
        </>
      ) : null}
    </nav>
  );
}
```
호출부(레일 인스턴스)에 `onOpenJournal={() => setIsJournalOpen(true)}` 전달(상태는 Task 8에서 추가). `rail-title` 제거로 인한 미사용 CSS 클래스는 Task 8에서 정리.

- [ ] **Step 3: activity-tagline 제거**

탭 아래 `<p className="activity-tagline">{profile.emoji} {profile.tagline}</p>` 블록을 완전히 삭제 — 탭/레일 바로 아래 `inner-tabs`가 오도록.

- [ ] **Step 4: 활동별 위치 기억**

`app/page.tsx` state에 추가:
```tsx
const [activityLocations, setActivityLocations] = useState<Partial<Record<ActivityKey, LocationPoint>>>({});
```
복원 useEffect(마운트 시):
```tsx
useEffect(() => {
  try {
    const raw = window.localStorage.getItem("running-alarm:activity-location:v1");
    if (raw) setActivityLocations(JSON.parse(raw));
  } catch {
    // 무시
  }
}, []);
```
저장 useEffect:
```tsx
useEffect(() => {
  try {
    window.localStorage.setItem("running-alarm:activity-location:v1", JSON.stringify(activityLocations));
  } catch {
    // 무시
  }
}, [activityLocations]);
```
`chooseLocation`(위치 확정 지점)에서 현재 활동 키에도 저장하도록 끝에 추가:
```tsx
function chooseLocation(next: LocationPoint, detail?: string) {
  setLocation(next);
  rememberLocation(next, detail);
  setActivityLocations((prev) => ({ ...prev, [activity]: next }));
  setDayMode("today");
  setIsSearchOpen(false);
  setSearchResults([]);
  setSearchQuery("");
  setSearchNote("");
}
```
`changeActivity`에서 활동 전환 시 저장된 위치가 있으면 전환(없으면 현재 유지):
```tsx
const changeActivity = useCallback((next: ActivityKey) => {
  setSheetKey(null);
  setIsOutfitOpen(false);
  setInnerTab("today");
  setActivity(next);
  setActivityLocations((prev) => {
    const remembered = prev[next];
    if (remembered) setLocation(remembered);
    return prev;
  });
}, []);
```
(GPS로 위치 갱신하는 경로도 동일하게 `setActivityLocations` 갱신 — GPS 성공 콜백에서 `setActivityLocations((prev) => ({ ...prev, [activity]: point }))` 추가. 정확한 위치는 `startLocate` 함수 내 위치 확정 지점에서 처리.)

- [ ] **Step 5: 검증**
  Run: `pnpm typecheck && pnpm build 2>&1 | tail -3`
  preview로: 등산에서 위치 검색 → 러닝으로 전환(위치 유지 또는 러닝의 마지막 위치) → 다시 등산으로 전환 시 아까 그 위치로 돌아오는지 확인.

- [ ] **Step 6: Commit**
  ```bash
  git add app/page.tsx
  git commit -m "feat: 좌측 레일 큰 글자+일지버튼, activity-tagline 제거, 활동별 위치 기억

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 8: JournalView (page.tsx) — 캘린더 + 상세 패널 + CSS

**Files:** `app/page.tsx`, `app/globals.css`

- [ ] **Step 1: state + import**

```tsx
import { emptyJournal, loadJournal, saveJournal, setNote, toggleJournalActivity, type Journal } from "@/lib/journal";
```
state:
```tsx
const [journal, setJournalState] = useState<Journal>(emptyJournal);
const [isJournalRestored, setIsJournalRestored] = useState(false);
const [isJournalOpen, setIsJournalOpen] = useState(false);
const [journalYm, setJournalYm] = useState(() => {
  const d = new Date();
  return { y: d.getFullYear(), m: d.getMonth() };
});
const [journalSelectedDate, setJournalSelectedDate] = useState<string | null>(null);
```
복원/저장:
```tsx
useEffect(() => {
  setJournalState(loadJournal());
  setIsJournalRestored(true);
}, []);

useEffect(() => {
  if (!isJournalRestored) return;
  saveJournal(journal);
}, [journal, isJournalRestored]);
```

- [ ] **Step 2: JournalView 컴포넌트**

page.tsx에 컴포넌트 추가(RecordView 근처):
```tsx
function JournalView({
  journal,
  ym,
  onShiftMonth,
  selectedDate,
  onSelectDate,
  onToggleActivity,
  onNoteChange,
  onClose
}: {
  journal: Journal;
  ym: { y: number; m: number };
  onShiftMonth: (delta: number) => void;
  selectedDate: string | null;
  onSelectDate: (date: string | null) => void;
  onToggleActivity: (date: string, activity: ActivityKey) => void;
  onNoteChange: (date: string, note: string) => void;
  onClose: () => void;
}) {
  const grid = buildMonthGrid(ym.y, ym.m);
  const weekdays = ["일", "월", "화", "수", "목", "금", "토"];
  const todayS = fmtDate(new Date());
  const entry = selectedDate ? journal[selectedDate] ?? { activities: [], note: "" } : null;

  return (
    <div className="sheet-backdrop" role="dialog" aria-modal="true" aria-label="운동 일지" onClick={onClose}>
      <div className="sheet journal-sheet" onClick={(event) => event.stopPropagation()}>
        <div className="sheet-topbar">
          <div className="sheet-grip" aria-hidden="true" />
          <button className="sheet-close" type="button" onClick={onClose} aria-label="닫기">
            <X size={18} />
          </button>
        </div>
        <div className="sheet-head">
          <p className="sheet-title">📔 나의 운동 일지</p>
        </div>

        <div className="cal-nav">
          <button type="button" onClick={() => onShiftMonth(-1)} aria-label="이전 달">
            <ChevronLeft size={18} />
          </button>
          <strong>{ym.y}년 {ym.m + 1}월</strong>
          <button type="button" onClick={() => onShiftMonth(1)} aria-label="다음 달">
            <ChevronRight size={18} />
          </button>
        </div>

        <div className="cal-grid journal-grid">
          {weekdays.map((w) => (
            <span className={`cal-wd ${w === "일" ? "sun" : w === "토" ? "sat" : ""}`} key={w}>
              {w}
            </span>
          ))}
          {grid.map((cell, i) => {
            if (!cell) return <span className="cal-cell empty" key={`e${i}`} />;
            const ds = fmtDate(cell);
            const dayEntry = journal[ds];
            const isToday = ds === todayS;
            const isSelected = ds === selectedDate;
            return (
              <button
                type="button"
                key={ds}
                onClick={() => onSelectDate(isSelected ? null : ds)}
                className={`cal-cell journal-cell ${isToday ? "is-today" : ""} ${isSelected ? "is-selected" : ""}`}
              >
                <span className="jc-date">{cell.getDate()}</span>
                {dayEntry && dayEntry.activities.length > 0 ? (
                  <span className="jc-icons">
                    {dayEntry.activities.slice(0, 3).map((a) => (
                      <span key={a} aria-hidden="true">{ACTIVITIES[a].emoji}</span>
                    ))}
                    {dayEntry.activities.length > 3 ? <span>+{dayEntry.activities.length - 3}</span> : null}
                  </span>
                ) : null}
              </button>
            );
          })}
        </div>

        {selectedDate && entry ? (
          <div className="journal-detail">
            <p className="journal-detail-date">{selectedDate}</p>
            <div className="journal-activity-picks">
              {ACTIVITY_ORDER.map((key) => {
                const on = entry.activities.includes(key);
                return (
                  <button
                    key={key}
                    type="button"
                    className={`journal-pick${on ? " on" : ""}`}
                    onClick={() => onToggleActivity(selectedDate, key)}
                  >
                    {ACTIVITIES[key].emoji} {ACTIVITIES[key].label}
                  </button>
                );
              })}
            </div>
            <textarea
              className="journal-note"
              placeholder="오늘 한 줄 남겨보세요 (예: 컨디션, 코스, 느낀 점)"
              value={entry.note}
              maxLength={200}
              onChange={(e) => onNoteChange(selectedDate, e.target.value)}
            />
          </div>
        ) : (
          <p className="journal-hint">날짜를 눌러 그날 한 활동과 한 줄 메모를 남겨보세요.</p>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 렌더 배선**

레일 인스턴스에 `onOpenJournal={() => setIsJournalOpen(true)}` 전달(Task 7에서 이미 props 추가). 시트 렌더 목록(다른 `{... ? <Sheet /> : null}` 근처)에 추가:
```tsx
{isJournalOpen ? (
  <JournalView
    journal={journal}
    ym={journalYm}
    onShiftMonth={(delta) => {
      const d = new Date(journalYm.y, journalYm.m + delta, 1);
      setJournalYm({ y: d.getFullYear(), m: d.getMonth() });
    }}
    selectedDate={journalSelectedDate}
    onSelectDate={setJournalSelectedDate}
    onToggleActivity={(date, act) => setJournalState((prev) => toggleJournalActivity(prev, date, act))}
    onNoteChange={(date, note) => setJournalState((prev) => setNote(prev, date, note))}
    onClose={() => {
      setIsJournalOpen(false);
      setJournalSelectedDate(null);
    }}
  />
) : null}
```
드로어 메뉴의 기존 "{활동} 기록 보기" 항목을 "📔 운동 일지 전체 보기"로 바꿔 `onClick`을 `() => { setIsMenuOpen(false); setIsJournalOpen(true); }`로 변경(활동별 상세 기록은 내부 탭 "기록"에서 계속 볼 수 있음 — 드로어는 전체 일지 진입점으로 통일).

- [ ] **Step 4: CSS**

`app/globals.css` 끝에 추가:
```css
/* 좌측 레일 — 큰 글자만 (v0.4.2) */
.activity-rail .act-item {
  padding: 13px 12px;
}

.act-rail-label {
  font-size: 1.02rem;
  font-weight: 850;
  color: var(--ink);
}

.rail-sep {
  height: 1px;
  background: var(--line);
  margin: 6px 4px;
}

.journal-btn {
  background: var(--brand-soft);
}

.journal-btn .act-rail-label {
  color: var(--brand-deep);
}

/* 애견 플랜 — 이유/대안/케어팁 */
.dog-reason {
  margin: 0;
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--ink-2);
}

.dog-alt {
  margin: 0;
  padding: 8px 11px;
  border-radius: 10px;
  background: var(--brand-soft);
  color: var(--brand-deep);
  font-size: 0.84rem;
  font-weight: 600;
}

.dog-care {
  margin: 0;
  font-size: 0.82rem;
  color: var(--muted);
}

/* 운동 일지 */
.journal-sheet {
  max-width: 480px;
}

.journal-grid.cal-grid {
  gap: 4px;
}

.journal-cell {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 6px 2px;
  min-height: 52px;
}

.jc-date {
  font-size: 0.82rem;
  font-weight: 700;
}

.jc-icons {
  display: flex;
  gap: 1px;
  font-size: 0.68rem;
}

.journal-cell.is-selected {
  outline: 2px solid var(--brand);
  border-radius: 10px;
}

.journal-detail {
  margin-top: 14px;
  padding-top: 14px;
  border-top: 1px solid var(--line);
}

.journal-detail-date {
  margin: 0 0 8px;
  font-size: 0.9rem;
  font-weight: 800;
}

.journal-activity-picks {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 10px;
}

.journal-pick {
  padding: 8px 12px;
  border-radius: 999px;
  border: 1.5px solid var(--line);
  background: #fff;
  font-size: 0.82rem;
  font-weight: 700;
  color: var(--ink-2);
}

.journal-pick.on {
  border-color: var(--brand);
  background: var(--brand-soft);
  color: var(--brand-deep);
}

.journal-note {
  width: 100%;
  min-height: 64px;
  padding: 10px 12px;
  border-radius: 12px;
  border: 1.5px solid var(--line);
  font-family: inherit;
  font-size: 0.86rem;
  resize: vertical;
}

.journal-hint {
  margin: 14px 2px 0;
  font-size: 0.82rem;
  color: var(--muted);
}
```

- [ ] **Step 5: 브라우저 검증**
  preview로: 일지 열기 → 날짜 클릭 → 활동 픽·메모 입력 → 닫고 새로고침 → 다시 열어서 유지되는지 확인. 모바일 375px에서 캘린더 셀 겹침 없는지 확인.

- [ ] **Step 6: Commit**
  ```bash
  git add app/page.tsx app/globals.css
  git commit -m "feat: 운동 일지 뷰(캘린더+활동체크+메모) 완성 — 재시작 후 유지 검증

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  ```

---

## Task 9: 최종 검증 + 문서 + v0.4.2 배포

**Files:** `package.json`, `CHANGELOG.md`, `docs/ROADMAP.md`, `docs/DEVELOPMENT_LOG.md`

- [ ] **Step 1: 전체 검증**
  Run: `pnpm test && pnpm typecheck && rm -rf .next && pnpm build`
  Expected: 골든마스터 포함 전부 통과.

- [ ] **Step 2: 전 활동·전 기기 육안 확인**
  preview 360/375/768/1024/1440에서: 5활동 순회, 애견 카드 이유/대안/케어팁, 등산 3구간, 가이드 동적 블록, 일지 저장·재시작 유지, 좌측 레일 큰 글자+일지버튼, activity-tagline 사라짐, 히어로 칩 모바일 2열. `grep -rn "composeBriefing\|findTomorrowMorningBest\|activity-tagline" app/ lib/` 결과 없음(또는 CSS 클래스 잔재 없음).

- [ ] **Step 3: 문서**
  - `package.json` `0.4.1` → `0.4.2`.
  - `CHANGELOG.md` `[0.4.2]`: 운동 일지, 동적 가이드, 애견 플랜 심화, 좌측 레일 정리, 등산 3구간, 활동별 위치, 죽은 코드 제거.
  - `ROADMAP.md`/`DEVELOPMENT_LOG.md` 갱신.

- [ ] **Step 4: 커밋·태그·배포**
  ```bash
  git add -A
  git commit -m "release: v0.4.2 — 운동 일지·동적 가이드·UI 정리

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  git tag -a v0.4.2 -m "v0.4.2 — 운동 일지 + 동적 가이드 + UI 정리 + 활동별 위치"
  git push origin main && git push origin v0.4.2
  ```

- [ ] **Step 5: 배포 검증**
  `curl -s -o /dev/null -w "%{http_code}" https://runningcall.vercel.app` → 200.

---

## Self-Review
- [ ] 스펙 9개 결정 대응: 칩(T2), 애견심화(T3), 일지(T4·T8), 등산3구간(T5), 동적가이드(T6), 레일+tagline제거+위치(T7), 죽은코드(T1).
- [ ] 러닝 골든마스터 불변(전 태스크 typecheck/test). 기존 activity-log 데이터 미변경.
- [ ] 타입 일관: JournalEntry/Journal, DogPlan 확장 필드, getDynamicGuideBlock 시그니처 태스크 간 일치.
- [ ] 일지 저장이 재시작 후 유지되는지 브라우저로 명시 검증(T8 Step5) — 사용자가 가장 강조한 요구.
